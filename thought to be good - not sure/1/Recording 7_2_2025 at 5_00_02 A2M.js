const fs = require('fs');
const puppeteer = require('puppeteer'); // v23.0.0 or later

(async () => {
    const browser = await puppeteer.launch();
    const page = await browser.newPage();
    const timeout = 5000;
    page.setDefaultTimeout(timeout);

    const lhApi = await import('lighthouse'); // v10.0.0 or later
    const flags = {
        screenEmulation: {
            disabled: true
        }
    }
    const config = lhApi.desktopConfig;
    const lhFlow = await lhApi.startFlow(page, {name: 'Recording 7/2/2025 at 5:00:02 AM', config, flags});
    {
        const targetPage = page;
        await targetPage.setViewport({
            width: 850,
            height: 669
        })
    }
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://moumi.com/');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const targetPage = page;
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[2];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Accept cookies)'),
            frame.locator('button.sp_choice_type_11'),
            frame.locator('::-p-xpath(//*[@id=\\"notice\\"]/div[2]/div[3]/div[2]/button[2])'),
            frame.locator(':scope >>> button.sp_choice_type_11')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 56.265625,
                y: 15.546875,
              },
            });
    }
    {
        const targetPage = page;
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Continue with Google)'),
            targetPage.locator("div:nth-of-type(3) > div:nth-of-type(1) > [data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> div:nth-of-type(3) > div:nth-of-type(1) > [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 146,
                y: 25.8125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Visual challenge. Audio challenge is available below, compatible with screen reader software.)'),
            frame.locator('div.sc-11w6f91-0 > button'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/button)'),
            frame.locator(':scope >>> div.sc-11w6f91-0 > button'),
            frame.locator('::-p-text(Start Puzzle)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 81,
                y: 26.2734375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 14,
                y: 19,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 14,
                y: 19,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 14,
                y: 19,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 14,
                y: 19,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 14,
                y: 19,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              count: 2,
              offset: {
                x: 14,
                y: 19,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to previous image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-3 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[1]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-3 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8,
                y: 7,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 15,
                y: 5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 15,
                y: 5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 15,
                y: 5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 15,
                y: 5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 15,
                y: 4,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Submit)'),
            frame.locator('div.sc-11w6f91-0 button'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/button)'),
            frame.locator(':scope >>> div.sc-11w6f91-0 button'),
            frame.locator('::-p-text(Submit)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 55.40625,
                y: 28,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 11,
                y: 8,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12,
                y: 9,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12,
                y: 9,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12,
                y: 9,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12,
                y: 9,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12,
                y: 9,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12,
                y: 8,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Submit)'),
            frame.locator('div.sc-11w6f91-0 button'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/button)'),
            frame.locator(':scope >>> div.sc-11w6f91-0 button'),
            frame.locator('::-p-text(Submit)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 38.40625,
                y: 9,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 23,
                y: 9,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              delay: 785.9000000003725,
              offset: {
                x: 23,
                y: 9,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 23,
                y: 9,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              delay: 685,
              offset: {
                x: 23,
                y: 9,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 23,
                y: 9,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 23,
                y: 9,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Submit)'),
            frame.locator('div.sc-11w6f91-0 button'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/button)'),
            frame.locator(':scope >>> div.sc-11w6f91-0 button'),
            frame.locator('::-p-text(Submit)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 33.40625,
                y: 19,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image)'),
            frame.locator('a.sc-7csxyx-4'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2])'),
            frame.locator(':scope >>> a.sc-7csxyx-4')
        ])
            .setTimeout(timeout)
            .click({
              delay: 983,
              offset: {
                x: 29,
                y: 24,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image)'),
            frame.locator('a.sc-7csxyx-4'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2])'),
            frame.locator(':scope >>> a.sc-7csxyx-4')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 29,
                y: 23,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image)'),
            frame.locator('a.sc-7csxyx-4'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2])'),
            frame.locator(':scope >>> a.sc-7csxyx-4')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 29,
                y: 23,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image)'),
            frame.locator('a.sc-7csxyx-4'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2])'),
            frame.locator(':scope >>> a.sc-7csxyx-4')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 29,
                y: 23,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Submit)'),
            frame.locator('div.sc-11w6f91-0 button'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/button)'),
            frame.locator(':scope >>> div.sc-11w6f91-0 button'),
            frame.locator('::-p-text(Submit)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 54.40625,
                y: 10,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 18,
                y: 12,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 18,
                y: 12,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 18,
                y: 12,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 18,
                y: 12,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 18,
                y: 12,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 18,
                y: 11,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 18,
                y: 11,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 19,
                y: 12,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Submit)'),
            frame.locator('div.sc-11w6f91-0 button'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/button)'),
            frame.locator(':scope >>> div.sc-11w6f91-0 button'),
            frame.locator('::-p-text(Submit)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 49.40625,
                y: 23,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Submit)'),
            frame.locator('div.sc-11w6f91-0 button'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/button)'),
            frame.locator(':scope >>> div.sc-11w6f91-0 button'),
            frame.locator('::-p-text(Submit)')
        ])
            .setTimeout(timeout)
            .click({
              delay: 1952.9000000003725,
              offset: {
                x: 44.40625,
                y: 18,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Submit)'),
            frame.locator('div.sc-11w6f91-0 button'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/button)'),
            frame.locator(':scope >>> div.sc-11w6f91-0 button'),
            frame.locator('::-p-text(Submit)')
        ])
            .setTimeout(timeout)
            .click({
              delay: 830.3000000007451,
              offset: {
                x: 38.40625,
                y: 22,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12,
                y: 15,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12,
                y: 15,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12,
                y: 15,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12,
                y: 15,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12,
                y: 15,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              delay: 424.5,
              offset: {
                x: 12,
                y: 15,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12,
                y: 15,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Submit)'),
            frame.locator('div.sc-11w6f91-0 button'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/button)'),
            frame.locator(':scope >>> div.sc-11w6f91-0 button'),
            frame.locator('::-p-text(Submit)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 61.40625,
                y: 11,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Submit)'),
            frame.locator('div.sc-11w6f91-0 button'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/button)'),
            frame.locator(':scope >>> div.sc-11w6f91-0 button'),
            frame.locator('::-p-text(Submit)')
        ])
            .setTimeout(timeout)
            .click({
              delay: 408.19999999925494,
              offset: {
                x: 53.40625,
                y: 6,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image) >>>> ::-p-aria([role=\\"image\\"])'),
            frame.locator('a.sc-7csxyx-4 > img'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2]/img)'),
            frame.locator(':scope >>> a.sc-7csxyx-4 > img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 14,
                y: 23,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image)'),
            frame.locator('a.sc-7csxyx-4'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2])'),
            frame.locator(':scope >>> a.sc-7csxyx-4')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 21,
                y: 27,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image)'),
            frame.locator('a.sc-7csxyx-4'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2])'),
            frame.locator(':scope >>> a.sc-7csxyx-4')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 21,
                y: 27,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image)'),
            frame.locator('a.sc-7csxyx-4'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2])'),
            frame.locator(':scope >>> a.sc-7csxyx-4')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 21,
                y: 27,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image)'),
            frame.locator('a.sc-7csxyx-4'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2])'),
            frame.locator(':scope >>> a.sc-7csxyx-4')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 21,
                y: 27,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Navigate to next image)'),
            frame.locator('a.sc-7csxyx-4'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/div/div[2]/div[1]/a[2])'),
            frame.locator(':scope >>> a.sc-7csxyx-4')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 21,
                y: 27,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://moumi-api.arkoselabs.com/v2/2.15.0/enforcement.cb91e3cd81d7571c86ff1f4512db1ade.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        let frame = targetPage.mainFrame();
        frame = frame.childFrames()[0];
        await puppeteer.Locator.race([
            frame.locator('::-p-aria(Submit)'),
            frame.locator('div.sc-11w6f91-0 button'),
            frame.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div[1]/div/button)'),
            frame.locator(':scope >>> div.sc-11w6f91-0 button'),
            frame.locator('::-p-text(Submit)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 50.40625,
                y: 20,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://accounts.google.com/v3/signin/identifier?opparams=%253F&dsh=S-251610084%3A1751425360955328&access_type=offline&client_id=203534559893-2tlko2sf4tl0fr0n11ddq3ji3m65s4h5.apps.googleusercontent.com&display=popup&o2v=1&redirect_uri=https%3A%2F%2Fmoumi.com%2Fexternal%2Fredirector.phtml&response_type=code&scope=https%3A%2F%2Fwww.googleapis.com%2Fauth%2Fuserinfo.profile+https%3A%2F%2Fwww.googleapis.com%2Fauth%2Fuserinfo.email&service=lso&state=&flowName=GeneralOAuthFlow&continue=https%3A%2F%2Faccounts.google.com%2Fsignin%2Foauth%2Fconsent%3Fauthuser%3Dunknown%26part%3DAJi8hAN-GPDBot3FzHCvIOjE-0a5Jhb6FCdtzRxAFGvqzPKKhSUKDCpeXZyWrrpmABz9841t4R4Azu0OZPnEHULEw4NehUv7o8g-qs1HWcShLK5xSGaLEyjCONIX0smtz-oSCOGUk0az8bK1ANFfICXtH3bwjJSkqTNgGlb_VAiDNMKkL0OtIUzdsWhpnSXAn86mKBzIjofDxmRW_ZzLL14QwbZwfADQooDcI-BzCqUrfTsidU0wMV5xNRDW7gMeAsgzJxdBO195xeSk3urIXg6PoRXlOGMe3Ff18YMAqTVYmtK4p-RZEo2UCUeoB6tmbqNDhyvzviTFL2PmG1RBEaryXJn70ggqxsS7RigqwuATDvsRoR_LO3nJPG9ldmsJAzAqnoL0u4R4xRH26vKOLHYHxT53wzqleYjeMkJZTmdSCr6ybThYvEtxAWqEByLAyutp1TEAWtkCBdCGYN5_HKplvTm7POlaqQ%26flowName%3DGeneralOAuthFlow%26as%3DS-251610084%253A1751425360955328%26client_id%3D203534559893-2tlko2sf4tl0fr0n11ddq3ji3m65s4h5.apps.googleusercontent.com%23&app_domain=https%3A%2F%2Fmoumi.com&rart=ANgoxcdh8hP8v8TcGewo_JE8BXprEQrbTOC6Oe96R-CrQBAuVXKtOxaXk83fbFlkleE_a4drCGTlaCHlKktoY0g2rOZprmLZ05JkplZSZ2PsArkSkFKzioQ', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Email or phone)'),
            targetPage.locator('#identifierId'),
            targetPage.locator('::-p-xpath(//*[@id=\\"identifierId\\"])'),
            targetPage.locator(':scope >>> #identifierId')
        ])
            .setTimeout(timeout)
            .fill('isla1new2');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://accounts.google.com/v3/signin/identifier?opparams=%253F&dsh=S-251610084%3A1751425360955328&access_type=offline&client_id=203534559893-2tlko2sf4tl0fr0n11ddq3ji3m65s4h5.apps.googleusercontent.com&display=popup&o2v=1&redirect_uri=https%3A%2F%2Fmoumi.com%2Fexternal%2Fredirector.phtml&response_type=code&scope=https%3A%2F%2Fwww.googleapis.com%2Fauth%2Fuserinfo.profile+https%3A%2F%2Fwww.googleapis.com%2Fauth%2Fuserinfo.email&service=lso&state=&flowName=GeneralOAuthFlow&continue=https%3A%2F%2Faccounts.google.com%2Fsignin%2Foauth%2Fconsent%3Fauthuser%3Dunknown%26part%3DAJi8hAN-GPDBot3FzHCvIOjE-0a5Jhb6FCdtzRxAFGvqzPKKhSUKDCpeXZyWrrpmABz9841t4R4Azu0OZPnEHULEw4NehUv7o8g-qs1HWcShLK5xSGaLEyjCONIX0smtz-oSCOGUk0az8bK1ANFfICXtH3bwjJSkqTNgGlb_VAiDNMKkL0OtIUzdsWhpnSXAn86mKBzIjofDxmRW_ZzLL14QwbZwfADQooDcI-BzCqUrfTsidU0wMV5xNRDW7gMeAsgzJxdBO195xeSk3urIXg6PoRXlOGMe3Ff18YMAqTVYmtK4p-RZEo2UCUeoB6tmbqNDhyvzviTFL2PmG1RBEaryXJn70ggqxsS7RigqwuATDvsRoR_LO3nJPG9ldmsJAzAqnoL0u4R4xRH26vKOLHYHxT53wzqleYjeMkJZTmdSCr6ybThYvEtxAWqEByLAyutp1TEAWtkCBdCGYN5_HKplvTm7POlaqQ%26flowName%3DGeneralOAuthFlow%26as%3DS-251610084%253A1751425360955328%26client_id%3D203534559893-2tlko2sf4tl0fr0n11ddq3ji3m65s4h5.apps.googleusercontent.com%23&app_domain=https%3A%2F%2Fmoumi.com&rart=ANgoxcdh8hP8v8TcGewo_JE8BXprEQrbTOC6Oe96R-CrQBAuVXKtOxaXk83fbFlkleE_a4drCGTlaCHlKktoY0g2rOZprmLZ05JkplZSZ2PsArkSkFKzioQ', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://accounts.google.com/v3/signin/identifier?opparams=%253F&dsh=S-251610084%3A1751425360955328&access_type=offline&client_id=203534559893-2tlko2sf4tl0fr0n11ddq3ji3m65s4h5.apps.googleusercontent.com&display=popup&o2v=1&redirect_uri=https%3A%2F%2Fmoumi.com%2Fexternal%2Fredirector.phtml&response_type=code&scope=https%3A%2F%2Fwww.googleapis.com%2Fauth%2Fuserinfo.profile+https%3A%2F%2Fwww.googleapis.com%2Fauth%2Fuserinfo.email&service=lso&state=&flowName=GeneralOAuthFlow&continue=https%3A%2F%2Faccounts.google.com%2Fsignin%2Foauth%2Fconsent%3Fauthuser%3Dunknown%26part%3DAJi8hAN-GPDBot3FzHCvIOjE-0a5Jhb6FCdtzRxAFGvqzPKKhSUKDCpeXZyWrrpmABz9841t4R4Azu0OZPnEHULEw4NehUv7o8g-qs1HWcShLK5xSGaLEyjCONIX0smtz-oSCOGUk0az8bK1ANFfICXtH3bwjJSkqTNgGlb_VAiDNMKkL0OtIUzdsWhpnSXAn86mKBzIjofDxmRW_ZzLL14QwbZwfADQooDcI-BzCqUrfTsidU0wMV5xNRDW7gMeAsgzJxdBO195xeSk3urIXg6PoRXlOGMe3Ff18YMAqTVYmtK4p-RZEo2UCUeoB6tmbqNDhyvzviTFL2PmG1RBEaryXJn70ggqxsS7RigqwuATDvsRoR_LO3nJPG9ldmsJAzAqnoL0u4R4xRH26vKOLHYHxT53wzqleYjeMkJZTmdSCr6ybThYvEtxAWqEByLAyutp1TEAWtkCBdCGYN5_HKplvTm7POlaqQ%26flowName%3DGeneralOAuthFlow%26as%3DS-251610084%253A1751425360955328%26client_id%3D203534559893-2tlko2sf4tl0fr0n11ddq3ji3m65s4h5.apps.googleusercontent.com%23&app_domain=https%3A%2F%2Fmoumi.com&rart=ANgoxcdh8hP8v8TcGewo_JE8BXprEQrbTOC6Oe96R-CrQBAuVXKtOxaXk83fbFlkleE_a4drCGTlaCHlKktoY0g2rOZprmLZ05JkplZSZ2PsArkSkFKzioQ', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://accounts.google.com/v3/signin/identifier?opparams=%253F&dsh=S-251610084%3A1751425360955328&access_type=offline&client_id=203534559893-2tlko2sf4tl0fr0n11ddq3ji3m65s4h5.apps.googleusercontent.com&display=popup&o2v=1&redirect_uri=https%3A%2F%2Fmoumi.com%2Fexternal%2Fredirector.phtml&response_type=code&scope=https%3A%2F%2Fwww.googleapis.com%2Fauth%2Fuserinfo.profile+https%3A%2F%2Fwww.googleapis.com%2Fauth%2Fuserinfo.email&service=lso&state=&flowName=GeneralOAuthFlow&continue=https%3A%2F%2Faccounts.google.com%2Fsignin%2Foauth%2Fconsent%3Fauthuser%3Dunknown%26part%3DAJi8hAN-GPDBot3FzHCvIOjE-0a5Jhb6FCdtzRxAFGvqzPKKhSUKDCpeXZyWrrpmABz9841t4R4Azu0OZPnEHULEw4NehUv7o8g-qs1HWcShLK5xSGaLEyjCONIX0smtz-oSCOGUk0az8bK1ANFfICXtH3bwjJSkqTNgGlb_VAiDNMKkL0OtIUzdsWhpnSXAn86mKBzIjofDxmRW_ZzLL14QwbZwfADQooDcI-BzCqUrfTsidU0wMV5xNRDW7gMeAsgzJxdBO195xeSk3urIXg6PoRXlOGMe3Ff18YMAqTVYmtK4p-RZEo2UCUeoB6tmbqNDhyvzviTFL2PmG1RBEaryXJn70ggqxsS7RigqwuATDvsRoR_LO3nJPG9ldmsJAzAqnoL0u4R4xRH26vKOLHYHxT53wzqleYjeMkJZTmdSCr6ybThYvEtxAWqEByLAyutp1TEAWtkCBdCGYN5_HKplvTm7POlaqQ%26flowName%3DGeneralOAuthFlow%26as%3DS-251610084%253A1751425360955328%26client_id%3D203534559893-2tlko2sf4tl0fr0n11ddq3ji3m65s4h5.apps.googleusercontent.com%23&app_domain=https%3A%2F%2Fmoumi.com&rart=ANgoxcdh8hP8v8TcGewo_JE8BXprEQrbTOC6Oe96R-CrQBAuVXKtOxaXk83fbFlkleE_a4drCGTlaCHlKktoY0g2rOZprmLZ05JkplZSZ2PsArkSkFKzioQ', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Enter your password)'),
            targetPage.locator('div.YqLCIe input'),
            targetPage.locator('::-p-xpath(//*[@id=\\"password\\"]/div[1]/div/div[1]/input)'),
            targetPage.locator(':scope >>> div.YqLCIe input')
        ])
            .setTimeout(timeout)
            .fill('L');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://accounts.google.com/v3/signin/identifier?opparams=%253F&dsh=S-251610084%3A1751425360955328&access_type=offline&client_id=203534559893-2tlko2sf4tl0fr0n11ddq3ji3m65s4h5.apps.googleusercontent.com&display=popup&o2v=1&redirect_uri=https%3A%2F%2Fmoumi.com%2Fexternal%2Fredirector.phtml&response_type=code&scope=https%3A%2F%2Fwww.googleapis.com%2Fauth%2Fuserinfo.profile+https%3A%2F%2Fwww.googleapis.com%2Fauth%2Fuserinfo.email&service=lso&state=&flowName=GeneralOAuthFlow&continue=https%3A%2F%2Faccounts.google.com%2Fsignin%2Foauth%2Fconsent%3Fauthuser%3Dunknown%26part%3DAJi8hAN-GPDBot3FzHCvIOjE-0a5Jhb6FCdtzRxAFGvqzPKKhSUKDCpeXZyWrrpmABz9841t4R4Azu0OZPnEHULEw4NehUv7o8g-qs1HWcShLK5xSGaLEyjCONIX0smtz-oSCOGUk0az8bK1ANFfICXtH3bwjJSkqTNgGlb_VAiDNMKkL0OtIUzdsWhpnSXAn86mKBzIjofDxmRW_ZzLL14QwbZwfADQooDcI-BzCqUrfTsidU0wMV5xNRDW7gMeAsgzJxdBO195xeSk3urIXg6PoRXlOGMe3Ff18YMAqTVYmtK4p-RZEo2UCUeoB6tmbqNDhyvzviTFL2PmG1RBEaryXJn70ggqxsS7RigqwuATDvsRoR_LO3nJPG9ldmsJAzAqnoL0u4R4xRH26vKOLHYHxT53wzqleYjeMkJZTmdSCr6ybThYvEtxAWqEByLAyutp1TEAWtkCBdCGYN5_HKplvTm7POlaqQ%26flowName%3DGeneralOAuthFlow%26as%3DS-251610084%253A1751425360955328%26client_id%3D203534559893-2tlko2sf4tl0fr0n11ddq3ji3m65s4h5.apps.googleusercontent.com%23&app_domain=https%3A%2F%2Fmoumi.com&rart=ANgoxcdh8hP8v8TcGewo_JE8BXprEQrbTOC6Oe96R-CrQBAuVXKtOxaXk83fbFlkleE_a4drCGTlaCHlKktoY0g2rOZprmLZ05JkplZSZ2PsArkSkFKzioQ', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('l');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://accounts.google.com/v3/signin/identifier?opparams=%253F&dsh=S-251610084%3A1751425360955328&access_type=offline&client_id=203534559893-2tlko2sf4tl0fr0n11ddq3ji3m65s4h5.apps.googleusercontent.com&display=popup&o2v=1&redirect_uri=https%3A%2F%2Fmoumi.com%2Fexternal%2Fredirector.phtml&response_type=code&scope=https%3A%2F%2Fwww.googleapis.com%2Fauth%2Fuserinfo.profile+https%3A%2F%2Fwww.googleapis.com%2Fauth%2Fuserinfo.email&service=lso&state=&flowName=GeneralOAuthFlow&continue=https%3A%2F%2Faccounts.google.com%2Fsignin%2Foauth%2Fconsent%3Fauthuser%3Dunknown%26part%3DAJi8hAN-GPDBot3FzHCvIOjE-0a5Jhb6FCdtzRxAFGvqzPKKhSUKDCpeXZyWrrpmABz9841t4R4Azu0OZPnEHULEw4NehUv7o8g-qs1HWcShLK5xSGaLEyjCONIX0smtz-oSCOGUk0az8bK1ANFfICXtH3bwjJSkqTNgGlb_VAiDNMKkL0OtIUzdsWhpnSXAn86mKBzIjofDxmRW_ZzLL14QwbZwfADQooDcI-BzCqUrfTsidU0wMV5xNRDW7gMeAsgzJxdBO195xeSk3urIXg6PoRXlOGMe3Ff18YMAqTVYmtK4p-RZEo2UCUeoB6tmbqNDhyvzviTFL2PmG1RBEaryXJn70ggqxsS7RigqwuATDvsRoR_LO3nJPG9ldmsJAzAqnoL0u4R4xRH26vKOLHYHxT53wzqleYjeMkJZTmdSCr6ybThYvEtxAWqEByLAyutp1TEAWtkCBdCGYN5_HKplvTm7POlaqQ%26flowName%3DGeneralOAuthFlow%26as%3DS-251610084%253A1751425360955328%26client_id%3D203534559893-2tlko2sf4tl0fr0n11ddq3ji3m65s4h5.apps.googleusercontent.com%23&app_domain=https%3A%2F%2Fmoumi.com&rart=ANgoxcdh8hP8v8TcGewo_JE8BXprEQrbTOC6Oe96R-CrQBAuVXKtOxaXk83fbFlkleE_a4drCGTlaCHlKktoY0g2rOZprmLZ05JkplZSZ2PsArkSkFKzioQ', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Enter your password)'),
            targetPage.locator('div.YqLCIe input'),
            targetPage.locator('::-p-xpath(//*[@id=\\"password\\"]/div[1]/div/div[1]/input)'),
            targetPage.locator(':scope >>> div.YqLCIe input')
        ])
            .setTimeout(timeout)
            .fill('Lune6891!!');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://accounts.google.com/v3/signin/identifier?opparams=%253F&dsh=S-251610084%3A1751425360955328&access_type=offline&client_id=203534559893-2tlko2sf4tl0fr0n11ddq3ji3m65s4h5.apps.googleusercontent.com&display=popup&o2v=1&redirect_uri=https%3A%2F%2Fmoumi.com%2Fexternal%2Fredirector.phtml&response_type=code&scope=https%3A%2F%2Fwww.googleapis.com%2Fauth%2Fuserinfo.profile+https%3A%2F%2Fwww.googleapis.com%2Fauth%2Fuserinfo.email&service=lso&state=&flowName=GeneralOAuthFlow&continue=https%3A%2F%2Faccounts.google.com%2Fsignin%2Foauth%2Fconsent%3Fauthuser%3Dunknown%26part%3DAJi8hAN-GPDBot3FzHCvIOjE-0a5Jhb6FCdtzRxAFGvqzPKKhSUKDCpeXZyWrrpmABz9841t4R4Azu0OZPnEHULEw4NehUv7o8g-qs1HWcShLK5xSGaLEyjCONIX0smtz-oSCOGUk0az8bK1ANFfICXtH3bwjJSkqTNgGlb_VAiDNMKkL0OtIUzdsWhpnSXAn86mKBzIjofDxmRW_ZzLL14QwbZwfADQooDcI-BzCqUrfTsidU0wMV5xNRDW7gMeAsgzJxdBO195xeSk3urIXg6PoRXlOGMe3Ff18YMAqTVYmtK4p-RZEo2UCUeoB6tmbqNDhyvzviTFL2PmG1RBEaryXJn70ggqxsS7RigqwuATDvsRoR_LO3nJPG9ldmsJAzAqnoL0u4R4xRH26vKOLHYHxT53wzqleYjeMkJZTmdSCr6ybThYvEtxAWqEByLAyutp1TEAWtkCBdCGYN5_HKplvTm7POlaqQ%26flowName%3DGeneralOAuthFlow%26as%3DS-251610084%253A1751425360955328%26client_id%3D203534559893-2tlko2sf4tl0fr0n11ddq3ji3m65s4h5.apps.googleusercontent.com%23&app_domain=https%3A%2F%2Fmoumi.com&rart=ANgoxcdh8hP8v8TcGewo_JE8BXprEQrbTOC6Oe96R-CrQBAuVXKtOxaXk83fbFlkleE_a4drCGTlaCHlKktoY0g2rOZprmLZ05JkplZSZ2PsArkSkFKzioQ', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://accounts.google.com/v3/signin/identifier?opparams=%253F&dsh=S-251610084%3A1751425360955328&access_type=offline&client_id=203534559893-2tlko2sf4tl0fr0n11ddq3ji3m65s4h5.apps.googleusercontent.com&display=popup&o2v=1&redirect_uri=https%3A%2F%2Fmoumi.com%2Fexternal%2Fredirector.phtml&response_type=code&scope=https%3A%2F%2Fwww.googleapis.com%2Fauth%2Fuserinfo.profile+https%3A%2F%2Fwww.googleapis.com%2Fauth%2Fuserinfo.email&service=lso&state=&flowName=GeneralOAuthFlow&continue=https%3A%2F%2Faccounts.google.com%2Fsignin%2Foauth%2Fconsent%3Fauthuser%3Dunknown%26part%3DAJi8hAN-GPDBot3FzHCvIOjE-0a5Jhb6FCdtzRxAFGvqzPKKhSUKDCpeXZyWrrpmABz9841t4R4Azu0OZPnEHULEw4NehUv7o8g-qs1HWcShLK5xSGaLEyjCONIX0smtz-oSCOGUk0az8bK1ANFfICXtH3bwjJSkqTNgGlb_VAiDNMKkL0OtIUzdsWhpnSXAn86mKBzIjofDxmRW_ZzLL14QwbZwfADQooDcI-BzCqUrfTsidU0wMV5xNRDW7gMeAsgzJxdBO195xeSk3urIXg6PoRXlOGMe3Ff18YMAqTVYmtK4p-RZEo2UCUeoB6tmbqNDhyvzviTFL2PmG1RBEaryXJn70ggqxsS7RigqwuATDvsRoR_LO3nJPG9ldmsJAzAqnoL0u4R4xRH26vKOLHYHxT53wzqleYjeMkJZTmdSCr6ybThYvEtxAWqEByLAyutp1TEAWtkCBdCGYN5_HKplvTm7POlaqQ%26flowName%3DGeneralOAuthFlow%26as%3DS-251610084%253A1751425360955328%26client_id%3D203534559893-2tlko2sf4tl0fr0n11ddq3ji3m65s4h5.apps.googleusercontent.com%23&app_domain=https%3A%2F%2Fmoumi.com&rart=ANgoxcdh8hP8v8TcGewo_JE8BXprEQrbTOC6Oe96R-CrQBAuVXKtOxaXk83fbFlkleE_a4drCGTlaCHlKktoY0g2rOZprmLZ05JkplZSZ2PsArkSkFKzioQ', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        const promises = [];
        const startWaitingForEvents = () => {
            promises.push(targetPage.waitForNavigation());
        }
        await targetPage.keyboard.up('Enter');
        await Promise.all(promises);
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://accounts.google.com/signin/oauth/id?authuser=0&part=AJi8hAPC3clslMpTXhSvlIkXkAiKe2pUFheDNjNpHB9q5SCsIAXqeoGMQvGuLu2edHeiAeg6k0VveEGMOBWVkK9EdSm8qB3Q2b5mGkIZgUhJRcgDhIkrhCNeAPIPgxRLcuYAkhQzCuT7OGX0UDUwyMRfT5gzKOuBe6JVTcrDmk7aTGlU65ivhea7C8wfjW7BQQDrHWgO7UifWtDsMId2yncdHReAvBErwBaz9XsPAjjHjv-giZSNUtQOP8PY0iyfoMwK_dIfY0uvDwHc9SZVfPUhfEXhz6qAP2Mg3FL4dgWSNhOy24rfR-er-OB4s-Riafb87i5khBhJzFf9ntRlVSzRYqxmQK-Y8by4RwWY2owTxslomwuIOFsr9RAfSPa78ifTL-QMyV0ozv1jv79c5oHlH98xnoZgisv85aecjA9EopkEpC8umF1Ku94Zm2i-a9hSyhycQspSR9c17ncRdDVWRHv5gHC-cHiq2Pre0wMXGPJNbnjPMMI9r8lstzO84NFBnwGsYJ88u72Pd6BED0GZvxOkOkLKTsJZYsV15w257EqRVIIz-TGmLtW4EC8e2fbTF78c8Zr27bJmy4YriQo3RfT7d0Iw8vwp9sJoDMxd0Es5GTvEKdLP13ZIOjTwdQ4z0fkV1_5TpwG6kueoXnv831izL4P8YKN8jFJgmCdkRkg6ALliAA6QF8qQcg2SLNds4AlUonitfVsCVzWTUrVY0RPUXXmhkc8NvDrDgffSk8I7jg4auzUoZ78TJRubbqiGzwKVawgzAD58m7BMb4VxoiF_Hlqru7L4m2qszdwUfYNMzwpjf-WLdTlHQkxteCuxsmS65RcUxQnOPX4p6BK3eoxi-IVxKg&flowName=GeneralOAuthFlow&as=S-251610084%3A1751425360955328&client_id=203534559893-2tlko2sf4tl0fr0n11ddq3ji3m65s4h5.apps.googleusercontent.com&rapt=AEjHL4O27JK19ErRV9MoTTHDa3KahDJsoJAblEmV54i6qMBwC6RtK0-OdR5gQG9nXpMw-tm6amocHaXHNQC8RPDR9d6vyGfqwo7Gk9QZDcPsoTPG1uPO1wA', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.JYXaTc > div > div > div:nth-of-type(2) div.VfPpkd-RLmnJb'),
            targetPage.locator('::-p-xpath(//*[@id=\\"yDmH0d\\"]/c-wiz/div/div[3]/div/div/div[2]/div/div/button/div[3])'),
            targetPage.locator(':scope >>> div.JYXaTc > div > div > div:nth-of-type(2) div.VfPpkd-RLmnJb')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 100,
                y: 8,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        const promises = [];
        const startWaitingForEvents = () => {
            promises.push(targetPage.waitForNavigation());
        }
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Zurück zu Moumi)'),
            targetPage.locator("[data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> [data-qa='button']")
        ])
            .setTimeout(timeout)
            .on('action', () => startWaitingForEvents())
            .click({
              offset: {
                x: 251.5,
                y: 23.03125,
              },
            });
        await Promise.all(promises);
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const targetPage = page;
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Foto aufnehmen)'),
            targetPage.locator("[data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 249.5,
                y: 26,
              },
            });
    }
    {
        const targetPage = page;
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Foto aufnehmen)'),
            targetPage.locator("[data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 248.5,
                y: 22.609375,
              },
            });
    }
    {
        const targetPage = page;
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Jetzt die App holen)'),
            targetPage.locator("[data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 195.5,
                y: 33.90625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Foto aufnehmen)'),
            targetPage.locator("[data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 245,
                y: 26,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Foto aufnehmen)'),
            targetPage.locator("[data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 208,
                y: 19.609375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Jetzt die App holen)'),
            targetPage.locator("[data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 216,
                y: 34.90625,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('chrome://settings/content/siteDetails?site=https%3A%2F%2Fam1.moumi.com');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'chrome://settings/content/siteDetails?site=https%3A%2F%2Fam1.moumi.com', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Camera)'),
            targetPage.locator('settings-ui >>>> #main >>>> settings-basic-page >>>> settings-privacy-page >>>> site-details >>>> site-details-permission:nth-of-type(2) >>>> #permission'),
            targetPage.locator(':scope >>> site-details-permission:nth-of-type(2) >>>> :scope >>> #permission')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 135.171875,
                y: 18.953125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'chrome://settings/content/siteDetails?site=https%3A%2F%2Fam1.moumi.com', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Camera)'),
            targetPage.locator('settings-ui >>>> #main >>>> settings-basic-page >>>> settings-privacy-page >>>> site-details >>>> site-details-permission:nth-of-type(2) >>>> #permission'),
            targetPage.locator(':scope >>> site-details-permission:nth-of-type(2) >>>> :scope >>> #permission')
        ])
            .setTimeout(timeout)
            .fill('allow');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Jetzt die App holen)'),
            targetPage.locator("[data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 238,
                y: 38.90625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Foto aufnehmen)'),
            targetPage.locator("[data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 237,
                y: 22,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Foto aufnehmen)'),
            targetPage.locator("[data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 251,
                y: 21.609375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Jetzt die App holen)'),
            targetPage.locator("[data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 213,
                y: 38.90625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Foto aufnehmen)'),
            targetPage.locator("[data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 287,
                y: 37,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Foto aufnehmen)'),
            targetPage.locator("[data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 251,
                y: 30.609375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Jetzt die App holen)'),
            targetPage.locator("[data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 266,
                y: 26.90625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Foto aufnehmen)'),
            targetPage.locator("[data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 348.5,
                y: 24,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Foto aufnehmen)'),
            targetPage.locator("[data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 348.5,
                y: 24,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Foto aufnehmen)'),
            targetPage.locator("[data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 383.5,
                y: 29.609375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Foto aufnehmen)'),
            targetPage.locator("[data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 383.5,
                y: 29.609375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Jetzt die App holen)'),
            targetPage.locator("[data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 305.5,
                y: 26.90625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Jetzt die App holen)'),
            targetPage.locator("[data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 305.5,
                y: 26.90625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('body'),
            targetPage.locator('::-p-xpath(/html/body)'),
            targetPage.locator(':scope >>> body')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 836,
                y: 101,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('body'),
            targetPage.locator('::-p-xpath(/html/body)'),
            targetPage.locator(':scope >>> body')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 836,
                y: 101,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Jetzt die App holen)'),
            targetPage.locator("[data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 325.5,
                y: 23.90625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Jetzt die App holen)'),
            targetPage.locator("[data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 325.5,
                y: 23.90625,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('chrome://new-tab-page/');
    }
    await lhFlow.endNavigation();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://www.google.com/search?q=sms+activate&oq=sms+activate&gs_lcrp=EgZjaHJvbWUyDggAEEUYJxg5GIAEGIoFMgcIARAAGIAEMgcIAhAAGIAEMgcIAxAAGIAEMgcIBBAAGIAEMgcIBRAAGIAEMgcIBhAAGIAEMgcIBxAAGIAEMgcICBAAGIAEMgcICRAAGIAE0gEINDgxMmowajeoAgCwAgA&sourceid=chrome&ie=UTF-8');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://www.google.com/search?q=sms+activate&oq=sms+activate&gs_lcrp=EgZjaHJvbWUyDggAEEUYJxg5GIAEGIoFMgcIARAAGIAEMgcIAhAAGIAEMgcIAxAAGIAEMgcIBBAAGIAEMgcIBRAAGIAEMgcIBhAAGIAEMgcIBxAAGIAEMgcICBAAGIAEMgcICRAAGIAE0gEINDgxMmowajeoAgCwAgA&sourceid=chrome&ie=UTF-8', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(SMS-Activate: Receive SMS Online to Virtual Number for OTP)'),
            targetPage.locator('#rso > div.MjjYud > div > div > div > div h3'),
            targetPage.locator('::-p-xpath(//*[@id=\\"rso\\"]/div[1]/div/div/div/div/div/div/div/div[1]/div/span/a/h3)'),
            targetPage.locator(':scope >>> #rso > div.MjjYud > div > div > div > div h3'),
            targetPage.locator('::-p-text(SMS-Activate:)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 327,
                y: 22.9609375,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://www.google.com/search?q=sms+activate&oq=sms+activate&gs_lcrp=EgZjaHJvbWUyDggAEEUYJxg5GIAEGIoFMgcIARAAGIAEMgcIAhAAGIAEMgcIAxAAGIAEMgcIBBAAGIAEMgcIBRAAGIAEMgcIBhAAGIAEMgcIBxAAGIAEMgcICBAAGIAEMgcICRAAGIAE0gEINDgxMmowajeoAgCwAgA&sourceid=chrome&ie=UTF-8', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        const promises = [];
        const startWaitingForEvents = () => {
            promises.push(targetPage.waitForNavigation());
        }
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(SMS-Activate: Receive SMS Online to Virtual Number for OTP)'),
            targetPage.locator('#rso > div.MjjYud > div > div > div > div h3'),
            targetPage.locator('::-p-xpath(//*[@id=\\"rso\\"]/div[1]/div/div/div/div/div/div/div/div[1]/div/span/a/h3)'),
            targetPage.locator(':scope >>> #rso > div.MjjYud > div > div > div > div h3'),
            targetPage.locator('::-p-text(SMS-Activate:)')
        ])
            .setTimeout(timeout)
            .on('action', () => startWaitingForEvents())
            .click({
              offset: {
                x: 327,
                y: 22.9609375,
              },
            });
        await Promise.all(promises);
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://sms-activate.io/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Try for free)'),
            targetPage.locator('section.main-page-hero a'),
            targetPage.locator('::-p-xpath(/html/body/div[9]/div/div[3]/section[1]/div/div/div/a)'),
            targetPage.locator(':scope >>> section.main-page-hero a'),
            targetPage.locator('::-p-text(Try for free)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 125.5,
                y: 31.0625,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://sms-activate.io/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        const promises = [];
        const startWaitingForEvents = () => {
            promises.push(targetPage.waitForNavigation());
        }
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Try for free)'),
            targetPage.locator('section.main-page-hero a'),
            targetPage.locator('::-p-xpath(/html/body/div[9]/div/div[3]/section[1]/div/div/div/a)'),
            targetPage.locator(':scope >>> section.main-page-hero a'),
            targetPage.locator('::-p-text(Try for free)')
        ])
            .setTimeout(timeout)
            .on('action', () => startWaitingForEvents())
            .click({
              offset: {
                x: 125.5,
                y: 31.0625,
              },
            });
        await Promise.all(promises);
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://sms-activate.io/freeNumbers', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Receive free SMS)'),
            targetPage.locator('div.content button.primary-second-btn'),
            targetPage.locator('::-p-xpath(/html/body/div[9]/div/div[3]/section/div/div/div[1]/div[1]/div/div/div/button[1])'),
            targetPage.locator(':scope >>> div.content button.primary-second-btn'),
            targetPage.locator('::-p-text(Receive free)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 115.5,
                y: 25.265625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://sms-activate.io/freeNumbers', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Receive free SMS)'),
            targetPage.locator('div.content button.primary-second-btn'),
            targetPage.locator('::-p-xpath(/html/body/div[9]/div/div[3]/section/div/div/div[1]/div[1]/div/div/div/button[1])'),
            targetPage.locator(':scope >>> div.content button.primary-second-btn'),
            targetPage.locator('::-p-text(Receive free)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 115.5,
                y: 25.265625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://sms-activate.io/freeNumbers', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('a:nth-of-type(7) > div.freeVirtualNumbersCountries__text'),
            targetPage.locator('::-p-xpath(/html/body/div[9]/div/div[3]/section/div/div/div[1]/div[3]/div/a[7]/div[2])'),
            targetPage.locator(':scope >>> a:nth-of-type(7) > div.freeVirtualNumbersCountries__text')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 90.171875,
                y: 23.7578125,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://sms-activate.io/freeNumbers', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        const promises = [];
        const startWaitingForEvents = () => {
            promises.push(targetPage.waitForNavigation());
        }
        await puppeteer.Locator.race([
            targetPage.locator('a:nth-of-type(7) > div.freeVirtualNumbersCountries__text'),
            targetPage.locator('::-p-xpath(/html/body/div[9]/div/div[3]/section/div/div/div[1]/div[3]/div/a[7]/div[2])'),
            targetPage.locator(':scope >>> a:nth-of-type(7) > div.freeVirtualNumbersCountries__text')
        ])
            .setTimeout(timeout)
            .on('action', () => startWaitingForEvents())
            .click({
              offset: {
                x: 90.171875,
                y: 23.7578125,
              },
            });
        await Promise.all(promises);
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Jetzt die App holen)'),
            targetPage.locator("[data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 351.5,
                y: 17.90625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Jetzt die App holen)'),
            targetPage.locator("[data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 351.5,
                y: 17.90625,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/de/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='cta-box-media'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"cta-box-media\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='cta-box-media'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 66.5,
                y: 70,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        const promises = [];
        const startWaitingForEvents = () => {
            promises.push(targetPage.waitForNavigation());
        }
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='cta-box-media'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"cta-box-media\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='cta-box-media'] > div")
        ])
            .setTimeout(timeout)
            .on('action', () => startWaitingForEvents())
            .click({
              offset: {
                x: 66.5,
                y: 70,
              },
            });
        await Promise.all(promises);
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Jetzt hinzufügen)'),
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div.csms-modal__scrollable > [data-qa='cta-box'] > [data-qa='cta-box-buttons'] > [data-qa='csms-stack'] > [data-qa='csms-stack-item'] > [data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div.csms-modal__scrollable > [data-qa='cta-box'] > [data-qa='cta-box-buttons'] > [data-qa='csms-stack'] > [data-qa='csms-stack-item'] > [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 321.5,
                y: 36.3046875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Jetzt hinzufügen)'),
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div.csms-modal__scrollable > [data-qa='cta-box'] > [data-qa='cta-box-buttons'] > [data-qa='csms-stack'] > [data-qa='csms-stack-item'] > [data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div.csms-modal__scrollable > [data-qa='cta-box'] > [data-qa='cta-box-buttons'] > [data-qa='csms-stack'] > [data-qa='csms-stack-item'] > [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 321.5,
                y: 36.3046875,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/de/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 24.166656494140625,
                y: 0.327972412109375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 24.166656494140625,
                y: 0.327972412109375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > [data-qa='action-sheet'] > [data-qa='action-sheet-list'] > [data-qa='action-sheet-list-item'] [data-qa='action-row-title']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"action-row-title\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > [data-qa='action-sheet'] > [data-qa='action-sheet-list'] > [data-qa='action-sheet-list-item'] [data-qa='action-row-title']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 26.5,
                y: 18,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > [data-qa='action-sheet'] > [data-qa='action-sheet-list'] > [data-qa='action-sheet-list-item'] [data-qa='action-row-title']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"action-row-title\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > [data-qa='action-sheet'] > [data-qa='action-sheet-list'] > [data-qa='action-sheet-list-item'] [data-qa='action-row-title']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 26.5,
                y: 18,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Jetzt Hallo sagen)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 99,
                y: 43.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Jetzt Hallo sagen)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 99,
                y: 43.3984375,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/de/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(4 / 5)'),
            targetPage.locator('div.modal-container button:nth-of-type(4)'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"star-rating\\"]/button[4])'),
            targetPage.locator(':scope >>> div.modal-container button:nth-of-type(4)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 11.5,
                y: 2.8984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(4 / 5)'),
            targetPage.locator('div.modal-container button:nth-of-type(4)'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"star-rating\\"]/button[4])'),
            targetPage.locator(':scope >>> div.modal-container button:nth-of-type(4)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 11.5,
                y: 2.8984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div.csms-modal__scrollable > [data-qa='rate-app-share'] > [data-qa='action-sheet-list-item'] [data-qa='action-row-title']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"action-row-title\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div.csms-modal__scrollable > [data-qa='rate-app-share'] > [data-qa='action-sheet-list-item'] [data-qa='action-row-title']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 289.5,
                y: 1,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div.csms-modal__scrollable > [data-qa='rate-app-share'] > [data-qa='action-sheet-list-item'] [data-qa='action-row-title']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"action-row-title\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div.csms-modal__scrollable > [data-qa='rate-app-share'] > [data-qa='action-sheet-list-item'] [data-qa='action-row-title']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 289.5,
                y: 1,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='modal-navigation'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='modal-navigation'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 6.5,
                y: 2.296875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='modal-navigation'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='modal-navigation'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 6.5,
                y: 2.296875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 15.166656494140625,
                y: 7.976898193359375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 15.166656494140625,
                y: 7.976898193359375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Jetzt Hallo sagen)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 143,
                y: 7.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Jetzt Hallo sagen)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 143,
                y: 7.3984375,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/de/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Profil)'),
            targetPage.locator("[data-qa='my-profile']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"my-profile\\"])'),
            targetPage.locator(":scope >>> [data-qa='my-profile']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 55.5,
                y: 32,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Profil)'),
            targetPage.locator("[data-qa='my-profile']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"my-profile\\"])'),
            targetPage.locator(":scope >>> [data-qa='my-profile']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 55.5,
                y: 32,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-secondary'] > div:nth-of-type(1) svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-secondary'] > div:nth-of-type(1) svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 4.5,
                y: 22,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-secondary'] > div:nth-of-type(1) svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-secondary'] > div:nth-of-type(1) svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 4.5,
                y: 22,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(2) > [data-qa='action-row'] [data-qa='action-row-title']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"action-row-title\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(2) > [data-qa='action-row'] [data-qa='action-row-title']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 57.5,
                y: 1,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(2) > [data-qa='action-row'] [data-qa='action-row-title']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"action-row-title\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(2) > [data-qa='action-row'] [data-qa='action-row-title']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 57.5,
                y: 1,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Zurück)'),
            targetPage.locator("[data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 22.5,
                y: 38,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Zurück)'),
            targetPage.locator("[data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 22.5,
                y: 38,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/de/settings');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(2) > [data-qa='action-row'] [data-qa='action-row-title'] > span"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"action-row-title\\"]/span)'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(2) > [data-qa='action-row'] [data-qa='action-row-title'] > span"),
            targetPage.locator('::-p-text(Profilsprache)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 58.5,
                y: 10.8046875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(2) > [data-qa='action-row'] [data-qa='action-row-title'] > span"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"action-row-title\\"]/span)'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(2) > [data-qa='action-row'] [data-qa='action-row-title'] > span"),
            targetPage.locator('::-p-text(Profilsprache)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 58.5,
                y: 10.8046875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("li:nth-of-type(2) [data-qa='action-row-title'] > span"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"action-row-title\\"]/span)'),
            targetPage.locator(":scope >>> li:nth-of-type(2) [data-qa='action-row-title'] > span"),
            targetPage.locator('::-p-text(English \\(United States\\))')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 52.5,
                y: 11.8046875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/de/encounters', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("li:nth-of-type(2) [data-qa='action-row-title'] > span"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"action-row-title\\"]/span)'),
            targetPage.locator(":scope >>> li:nth-of-type(2) [data-qa='action-row-title'] > span"),
            targetPage.locator('::-p-text(English \\(United States\\))')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 52.5,
                y: 11.8046875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-header'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='screen-header'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12.5,
                y: 22,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-header'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='screen-header'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12.5,
                y: 22,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(99+ Likes) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='liked-you'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='liked-you'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 1.500244140625,
                y: 7.0025634765625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(99+ Likes) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='liked-you'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='liked-you'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 1.500244140625,
                y: 7.0025634765625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Profile)'),
            targetPage.locator("[data-qa='my-profile']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"my-profile\\"])'),
            targetPage.locator(":scope >>> [data-qa='my-profile']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 45.5,
                y: 21,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Profile)'),
            targetPage.locator("[data-qa='my-profile']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"my-profile\\"])'),
            targetPage.locator(":scope >>> [data-qa='my-profile']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 45.5,
                y: 21,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Settings)'),
            targetPage.locator("[data-qa='navbar-slot-secondary'] > div:nth-of-type(1) [data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-secondary'] > div:nth-of-type(1) [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 28.5,
                y: 38,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Settings)'),
            targetPage.locator("[data-qa='navbar-slot-secondary'] > div:nth-of-type(1) [data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-secondary'] > div:nth-of-type(1) [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 28.5,
                y: 38,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(2) > [data-qa='action-row'] [data-qa='action-row-title'] > span"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"action-row-title\\"]/span)'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(2) > [data-qa='action-row'] [data-qa='action-row-title'] > span"),
            targetPage.locator('::-p-text(Account)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 42.5,
                y: 15.8046875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(2) > [data-qa='action-row'] [data-qa='action-row-title'] > span"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"action-row-title\\"]/span)'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(2) > [data-qa='action-row'] [data-qa='action-row-title'] > span"),
            targetPage.locator('::-p-text(Account)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 42.5,
                y: 15.8046875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Delete account)'),
            targetPage.locator("[data-qa='delete-account-button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"delete-account-button\\"])'),
            targetPage.locator(":scope >>> [data-qa='delete-account-button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 269.5,
                y: 24.9921875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Delete account)'),
            targetPage.locator("[data-qa='delete-account-button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"delete-account-button\\"])'),
            targetPage.locator(":scope >>> [data-qa='delete-account-button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 269.5,
                y: 24.9921875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div.csms-screen__block--hpadded > div > [data-qa='form-control'] > [data-qa='csms-stack'] > [data-qa='csms-stack-item'] span.csms-action-cell__title > span"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"alternative_6\\"]/span[1]/span)'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div.csms-screen__block--hpadded > div > [data-qa='form-control'] > [data-qa='csms-stack'] > [data-qa='csms-stack-item'] span.csms-action-cell__title > span"),
            targetPage.locator('::-p-text(Delete your account)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 36.5,
                y: 2.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div.csms-screen__block--hpadded > div > [data-qa='form-control'] > [data-qa='csms-stack'] > [data-qa='csms-stack-item'] span.csms-action-cell__title > span"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"alternative_6\\"]/span[1]/span)'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div.csms-screen__block--hpadded > div > [data-qa='form-control'] > [data-qa='csms-stack'] > [data-qa='csms-stack-item'] span.csms-action-cell__title > span"),
            targetPage.locator('::-p-text(Delete your account)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 36.5,
                y: 2.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Continue)'),
            targetPage.locator("[data-qa='delete-account-alternative-button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"delete-account-alternative-button\\"])'),
            targetPage.locator(":scope >>> [data-qa='delete-account-alternative-button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 112.5,
                y: 21.6015625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Continue)'),
            targetPage.locator("[data-qa='delete-account-alternative-button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"delete-account-alternative-button\\"])'),
            targetPage.locator(":scope >>> [data-qa='delete-account-alternative-button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 112.5,
                y: 21.6015625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Free Moumi Premium)'),
            targetPage.locator("[data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 70.5,
                y: 19.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Free Moumi Premium)'),
            targetPage.locator("[data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 70.5,
                y: 19.40625,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(99+ Likes) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='liked-you'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='liked-you'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 5.500244140625,
                y: 6.0025634765625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(99+ Likes) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='liked-you'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='liked-you'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 5.500244140625,
                y: 6.0025634765625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Like Oniel back)'),
            targetPage.locator('div.csms-screen__block--align-stretch > div > div > div:nth-of-type(1) button:nth-of-type(2)'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/div/button[2])'),
            targetPage.locator(':scope >>> div.csms-screen__block--align-stretch > div > div > div:nth-of-type(1) button:nth-of-type(2)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 70,
                y: 33.734375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Like Oniel back)'),
            targetPage.locator('div.csms-screen__block--align-stretch > div > div > div:nth-of-type(1) button:nth-of-type(2)'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/div/button[2])'),
            targetPage.locator(':scope >>> div.csms-screen__block--align-stretch > div > div > div:nth-of-type(1) button:nth-of-type(2)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 70,
                y: 33.734375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-footer'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen-footer\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='screen-footer'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 181.5,
                y: 240.390625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-footer'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen-footer\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='screen-footer'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 181.5,
                y: 240.390625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 142.5,
                y: 16,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 142.5,
                y: 16,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 77.5,
                y: 17,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 77.5,
                y: 17,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 67.5,
                y: 9,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 67.5,
                y: 9,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 68.5,
                y: 10,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 68.5,
                y: 10,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              count: 2,
              offset: {
                x: 91.5,
                y: 9,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              count: 2,
              offset: {
                x: 91.5,
                y: 9,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('hi');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-primary'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 18.5,
                y: 22,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-primary'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 18.5,
                y: 22,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/liked-you');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div:nth-of-type(8) span.csms-user-card-content__slots'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/button/span[2])'),
            targetPage.locator(':scope >>> div:nth-of-type(8) span.csms-user-card-content__slots')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 61.5,
                y: 208.90625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div:nth-of-type(8) span.csms-user-card-content__slots'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/button/span[2])'),
            targetPage.locator(':scope >>> div:nth-of-type(8) span.csms-user-card-content__slots')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 61.5,
                y: 208.90625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 119.5,
                y: 232,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 119.5,
                y: 232,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the next photo)'),
            targetPage.locator('div.slider-gallery button'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button)'),
            targetPage.locator(':scope >>> div.slider-gallery button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 20,
                y: 354,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the next photo)'),
            targetPage.locator('div.slider-gallery button'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button)'),
            targetPage.locator(':scope >>> div.slider-gallery button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 20,
                y: 354,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(InkedD’s photo, 2 of 3)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 833,
                y: 335,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(InkedD’s photo, 2 of 3)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 833,
                y: 335,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('button.slider-gallery-controls__control--next svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> button.slider-gallery-controls__control--next svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3,
                y: 7.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('button.slider-gallery-controls__control--next svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> button.slider-gallery-controls__control--next svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3,
                y: 7.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the previous photo)'),
            targetPage.locator('div.slider-gallery button'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button)'),
            targetPage.locator(':scope >>> div.slider-gallery button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 23,
                y: 318,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the previous photo)'),
            targetPage.locator('div.slider-gallery button'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button)'),
            targetPage.locator(':scope >>> div.slider-gallery button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 23,
                y: 318,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(InkedD’s photo, 2 of 3)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 118,
                y: 226,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(InkedD’s photo, 2 of 3)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 118,
                y: 226,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(InkedD’s photo, 2 of 3)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 114,
                y: 281,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(InkedD’s photo, 2 of 3)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 114,
                y: 281,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(13 Chats)'),
            targetPage.locator("[data-qa='connections']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections\\"])'),
            targetPage.locator(":scope >>> [data-qa='connections']"),
            targetPage.locator('::-p-text(13Chats)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 50.5,
                y: 16,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(13 Chats)'),
            targetPage.locator("[data-qa='connections']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections\\"])'),
            targetPage.locator(":scope >>> [data-qa='connections']"),
            targetPage.locator('::-p-text(13Chats)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 50.5,
                y: 16,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Reply to Sina)'),
            targetPage.locator("[data-qa='quick-chat-text-input']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"quick-chat-text-input\\"])'),
            targetPage.locator(":scope >>> [data-qa='quick-chat-text-input']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 43.5,
                y: 4.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Reply to Sina)'),
            targetPage.locator("[data-qa='quick-chat-text-input']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"quick-chat-text-input\\"])'),
            targetPage.locator(":scope >>> [data-qa='quick-chat-text-input']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 43.5,
                y: 4.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Reply to Sina)'),
            targetPage.locator("[data-qa='quick-chat-text-input']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"quick-chat-text-input\\"])'),
            targetPage.locator(":scope >>> [data-qa='quick-chat-text-input']")
        ])
            .setTimeout(timeout)
            .fill('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Reply to Sina)'),
            targetPage.locator("[data-qa='quick-chat-text-input']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"quick-chat-text-input\\"])'),
            targetPage.locator(":scope >>> [data-qa='quick-chat-text-input']")
        ])
            .setTimeout(timeout)
            .fill('hi');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen'] > div > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen\\"]/div/div)'),
            targetPage.locator(":scope >>> [data-qa='screen'] > div > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 179.5,
                y: 527,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen'] > div > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen\\"]/div/div)'),
            targetPage.locator(":scope >>> [data-qa='screen'] > div > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 179.5,
                y: 527,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Reply to N)'),
            targetPage.locator("[data-qa='quick-chat-text-input']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"quick-chat-text-input\\"])'),
            targetPage.locator(":scope >>> [data-qa='quick-chat-text-input']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 58.5,
                y: 28.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Reply to N)'),
            targetPage.locator("[data-qa='quick-chat-text-input']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"quick-chat-text-input\\"])'),
            targetPage.locator(":scope >>> [data-qa='quick-chat-text-input']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 58.5,
                y: 28.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Reply to N)'),
            targetPage.locator("[data-qa='quick-chat-text-input']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"quick-chat-text-input\\"])'),
            targetPage.locator(":scope >>> [data-qa='quick-chat-text-input']")
        ])
            .setTimeout(timeout)
            .fill('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Reply to N)'),
            targetPage.locator("[data-qa='quick-chat-text-input']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"quick-chat-text-input\\"])'),
            targetPage.locator(":scope >>> [data-qa='quick-chat-text-input']")
        ])
            .setTimeout(timeout)
            .fill('hi');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("div.csms-screen__block--align-center > div > div > div:nth-of-type(1) [data-qa='profile-info__name'] > span"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"profile-info__name\\"]/span)'),
            targetPage.locator(":scope >>> div.csms-screen__block--align-center > div > div > div:nth-of-type(1) [data-qa='profile-info__name'] > span")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 17.712493896484375,
                y: 1.4453125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("div.csms-screen__block--align-center > div > div > div:nth-of-type(1) [data-qa='profile-info__name'] > span"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"profile-info__name\\"]/span)'),
            targetPage.locator(":scope >>> div.csms-screen__block--align-center > div > div > div:nth-of-type(1) [data-qa='profile-info__name'] > span")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 17.712493896484375,
                y: 1.4453125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Han’s main photo)'),
            targetPage.locator("div.csms-screen__block--align-center > div > div > div:nth-of-type(1) [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> div.csms-screen__block--align-center > div > div > div:nth-of-type(1) [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 83.89999389648438,
                y: 237.15625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Han’s main photo)'),
            targetPage.locator("div.csms-screen__block--align-center > div > div > div:nth-of-type(1) [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> div.csms-screen__block--align-center > div > div > div:nth-of-type(1) [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 83.89999389648438,
                y: 237.15625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Han’s main photo)'),
            targetPage.locator("div.csms-screen__block--align-center > div > div > div:nth-of-type(1) [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> div.csms-screen__block--align-center > div > div > div:nth-of-type(1) [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 91.89999389648438,
                y: 278.15625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Han’s main photo)'),
            targetPage.locator("div.csms-screen__block--align-center > div > div > div:nth-of-type(1) [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> div.csms-screen__block--align-center > div > div > div:nth-of-type(1) [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 91.89999389648438,
                y: 278.15625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('z');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('z');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('z');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('z');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Reply to Han)'),
            targetPage.locator("[data-qa='quick-chat-text-input']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"quick-chat-text-input\\"])'),
            targetPage.locator(":scope >>> [data-qa='quick-chat-text-input']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 58.5,
                y: 22.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Reply to Han)'),
            targetPage.locator("[data-qa='quick-chat-text-input']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"quick-chat-text-input\\"])'),
            targetPage.locator(":scope >>> [data-qa='quick-chat-text-input']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 58.5,
                y: 22.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Reply to Han)'),
            targetPage.locator("[data-qa='quick-chat-text-input']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"quick-chat-text-input\\"])'),
            targetPage.locator(":scope >>> [data-qa='quick-chat-text-input']")
        ])
            .setTimeout(timeout)
            .fill('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Reply to Han)'),
            targetPage.locator("[data-qa='quick-chat-text-input']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"quick-chat-text-input\\"])'),
            targetPage.locator(":scope >>> [data-qa='quick-chat-text-input']")
        ])
            .setTimeout(timeout)
            .fill('he');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Reply to Han)'),
            targetPage.locator("[data-qa='quick-chat-text-input']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"quick-chat-text-input\\"])'),
            targetPage.locator(":scope >>> [data-qa='quick-chat-text-input']")
        ])
            .setTimeout(timeout)
            .fill('hey');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('y');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Reply to Han)'),
            targetPage.locator("[data-qa='quick-chat-text-input']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"quick-chat-text-input\\"])'),
            targetPage.locator(":scope >>> [data-qa='quick-chat-text-input']")
        ])
            .setTimeout(timeout)
            .fill('hey ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Reply to Han)'),
            targetPage.locator("[data-qa='quick-chat-text-input']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"quick-chat-text-input\\"])'),
            targetPage.locator(":scope >>> [data-qa='quick-chat-text-input']")
        ])
            .setTimeout(timeout)
            .fill('hey u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Reply to Douggy)'),
            targetPage.locator("[data-qa='quick-chat-text-input']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"quick-chat-text-input\\"])'),
            targetPage.locator(":scope >>> [data-qa='quick-chat-text-input']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 185.5,
                y: 3.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Reply to Douggy)'),
            targetPage.locator("[data-qa='quick-chat-text-input']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"quick-chat-text-input\\"])'),
            targetPage.locator(":scope >>> [data-qa='quick-chat-text-input']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 185.5,
                y: 3.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Skip)'),
            targetPage.locator("[data-qa='mini-game-skip']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mini-game-skip\\"])'),
            targetPage.locator(":scope >>> [data-qa='mini-game-skip']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 197.5,
                y: 11.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Skip)'),
            targetPage.locator("[data-qa='mini-game-skip']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mini-game-skip\\"])'),
            targetPage.locator(":scope >>> [data-qa='mini-game-skip']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 197.5,
                y: 11.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Skip)'),
            targetPage.locator("[data-qa='mini-game-skip']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mini-game-skip\\"])'),
            targetPage.locator(":scope >>> [data-qa='mini-game-skip']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 182.5,
                y: 31.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Skip)'),
            targetPage.locator("[data-qa='mini-game-skip']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mini-game-skip\\"])'),
            targetPage.locator(":scope >>> [data-qa='mini-game-skip']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 182.5,
                y: 31.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Start Swiping)'),
            targetPage.locator("[data-qa='mini-game-end-primary']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mini-game-end-primary\\"])'),
            targetPage.locator(":scope >>> [data-qa='mini-game-end-primary']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 172.5,
                y: 49.3515625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Start Swiping)'),
            targetPage.locator("[data-qa='mini-game-end-primary']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mini-game-end-primary\\"])'),
            targetPage.locator(":scope >>> [data-qa='mini-game-end-primary']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 172.5,
                y: 49.3515625,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 26.0013427734375,
                y: 15.40643310546875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 26.0013427734375,
                y: 15.40643310546875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 130,
                y: 27.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 130,
                y: 27.3984375,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='my-profile'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='my-profile'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 19.5,
                y: 23,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='my-profile'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='my-profile'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 19.5,
                y: 23,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('circle.csms-progress-circle__svg-path-indicator'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"brick-action\\"]/span[2]/div/span/svg/circle[2])'),
            targetPage.locator(':scope >>> circle.csms-progress-circle__svg-path-indicator')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 56.445465087890625,
                y: 58.945457458496094,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('circle.csms-progress-circle__svg-path-indicator'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"brick-action\\"]/span[2]/div/span/svg/circle[2])'),
            targetPage.locator(':scope >>> circle.csms-progress-circle__svg-path-indicator')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 56.445465087890625,
                y: 58.945457458496094,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Tania’s main photo[role=\\"image\\"])'),
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 289.5,
                y: 308,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Tania’s main photo[role=\\"image\\"])'),
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 289.5,
                y: 308,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the next photo)'),
            targetPage.locator('div.slider-gallery button'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button)'),
            targetPage.locator(':scope >>> div.slider-gallery button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 6,
                y: 362,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the next photo)'),
            targetPage.locator('div.slider-gallery button'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button)'),
            targetPage.locator(':scope >>> div.slider-gallery button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 6,
                y: 362,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.slider-gallery svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> div.slider-gallery svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 17,
                y: 27.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.slider-gallery svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> div.slider-gallery svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 17,
                y: 27.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Tania’s photo, 1 of 2)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 88,
                y: 195,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Tania’s photo, 1 of 2)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 88,
                y: 195,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/own-profile/preview-profile');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.modal-container > div > div > div > button'),
            targetPage.locator('::-p-xpath(//*[@id=\\"app-root\\"]/div/div[4]/div/div/div/button)'),
            targetPage.locator(':scope >>> div.modal-container > div > div > div > button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 100,
                y: 113,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.modal-container > div > div > div > button'),
            targetPage.locator('::-p-xpath(//*[@id=\\"app-root\\"]/div/div[4]/div/div/div/button)'),
            targetPage.locator(':scope >>> div.modal-container > div > div > div > button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 100,
                y: 113,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='people-nearby'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='people-nearby'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 5.5,
                y: 21,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='people-nearby'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='people-nearby'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 5.5,
                y: 21,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Cameron, 27 years old, Online now) >>>> ::-p-aria([role=\\"image\\"])'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(3) > div.people-nearby__content > [data-qa='people-nearby-content'] li:nth-of-type(1) [data-qa='avatar__image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"avatar__image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(3) > div.people-nearby__content > [data-qa='people-nearby-content'] li:nth-of-type(1) [data-qa='avatar__image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 75,
                y: 139,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Cameron, 27 years old, Online now) >>>> ::-p-aria([role=\\"image\\"])'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(3) > div.people-nearby__content > [data-qa='people-nearby-content'] li:nth-of-type(1) [data-qa='avatar__image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"avatar__image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(3) > div.people-nearby__content > [data-qa='people-nearby-content'] li:nth-of-type(1) [data-qa='avatar__image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 75,
                y: 139,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 119.5,
                y: 296,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 119.5,
                y: 296,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Cameron’s photo, 1 of 4)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 818,
                y: 341,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Cameron’s photo, 1 of 4)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 818,
                y: 341,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the next photo)'),
            targetPage.locator('div.slider-gallery button'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button)'),
            targetPage.locator(':scope >>> div.slider-gallery button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 18,
                y: 360,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the next photo)'),
            targetPage.locator('div.slider-gallery button'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button)'),
            targetPage.locator(':scope >>> div.slider-gallery button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 18,
                y: 360,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Cameron’s photo, 2 of 4)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 828,
                y: 336,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Cameron’s photo, 2 of 4)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 828,
                y: 336,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the next photo)'),
            targetPage.locator('button.slider-gallery-controls__control--next'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button[2])'),
            targetPage.locator(':scope >>> button.slider-gallery-controls__control--next')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 41,
                y: 361,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the next photo)'),
            targetPage.locator('button.slider-gallery-controls__control--next'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button[2])'),
            targetPage.locator(':scope >>> button.slider-gallery-controls__control--next')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 41,
                y: 361,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/people-nearby/zAhMACjIxMDcyODgxODYIJFv5hQAAAAAgDDm4qwwKORBG2epTHgLonqVKmCDhZctjv6pA6ysZYBQ');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 105.5,
                y: 235,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 105.5,
                y: 235,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Cameron’s photo, 1 of 4)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 88,
                y: 133,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Cameron’s photo, 1 of 4)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 88,
                y: 133,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/people-nearby/zAhMACjIxMDcyODgxODYIJFv5hQAAAAAgDDm4qwwKORBG2epTHgLonqVKmCDhZctjv6pA6ysZYBQ');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.modal-container > div > div > div > button'),
            targetPage.locator('::-p-xpath(//*[@id=\\"app-root\\"]/div/div[4]/div/div/div/button)'),
            targetPage.locator(':scope >>> div.modal-container > div > div > div > button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 93,
                y: 225,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.modal-container > div > div > div > button'),
            targetPage.locator('::-p-xpath(//*[@id=\\"app-root\\"]/div/div[4]/div/div/div/button)'),
            targetPage.locator(':scope >>> div.modal-container > div > div > div > button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 93,
                y: 225,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/own-profile');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Cameron, 27 years old, Online now) >>>> ::-p-aria([role=\\"image\\"])'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(3) > div.people-nearby__content > [data-qa='people-nearby-content'] li:nth-of-type(1) [data-qa='avatar__image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"avatar__image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(3) > div.people-nearby__content > [data-qa='people-nearby-content'] li:nth-of-type(1) [data-qa='avatar__image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 67,
                y: 172,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Cameron, 27 years old, Online now) >>>> ::-p-aria([role=\\"image\\"])'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(3) > div.people-nearby__content > [data-qa='people-nearby-content'] li:nth-of-type(1) [data-qa='avatar__image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"avatar__image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(3) > div.people-nearby__content > [data-qa='people-nearby-content'] li:nth-of-type(1) [data-qa='avatar__image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 67,
                y: 172,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 172.5,
                y: 236,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 172.5,
                y: 236,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Cameron’s photo, 1 of 4)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 226,
                y: 265,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Cameron’s photo, 1 of 4)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 226,
                y: 265,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Cameron’s photo, 1 of 4)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 85,
                y: 156,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Cameron’s photo, 1 of 4)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 85,
                y: 156,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Cameron’s photo, 1 of 4)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 31,
                y: 69,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Cameron’s photo, 1 of 4)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 31,
                y: 69,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Cameron’s photo, 1 of 4)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 174,
                y: 126,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Cameron’s photo, 1 of 4)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 174,
                y: 126,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/people-nearby/zAhMACjIxMDcyODgxODYIJFv5hQAAAAAgDDm4qwwKORBG2epTHgLonqVKmCDhZctjv6pA6ysZYBQ');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 19.166656494140625,
                y: 9.723052978515625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 19.166656494140625,
                y: 9.723052978515625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.modal-container > div > div > div > button'),
            targetPage.locator('::-p-xpath(//*[@id=\\"app-root\\"]/div/div[4]/div/div/div/button)'),
            targetPage.locator(':scope >>> div.modal-container > div > div > div > button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 130,
                y: 228,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.modal-container > div > div > div > button'),
            targetPage.locator('::-p-xpath(//*[@id=\\"app-root\\"]/div/div[4]/div/div/div/button)'),
            targetPage.locator(':scope >>> div.modal-container > div > div > div > button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 130,
                y: 228,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/own-profile');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Shaun, 27 years old, Online now) >>>> ::-p-aria([role=\\"image\\"])'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(3) > div.people-nearby__content > [data-qa='people-nearby-content'] li:nth-of-type(2) [data-qa='avatar__image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"avatar__image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(3) > div.people-nearby__content > [data-qa='people-nearby-content'] li:nth-of-type(2) [data-qa='avatar__image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 38.3359375,
                y: 189,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Shaun, 27 years old, Online now) >>>> ::-p-aria([role=\\"image\\"])'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(3) > div.people-nearby__content > [data-qa='people-nearby-content'] li:nth-of-type(2) [data-qa='avatar__image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"avatar__image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(3) > div.people-nearby__content > [data-qa='people-nearby-content'] li:nth-of-type(2) [data-qa='avatar__image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 38.3359375,
                y: 189,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 108.5,
                y: 247,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 108.5,
                y: 247,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Shaun’s photo, 1 of 5)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 394,
                y: 376,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Shaun’s photo, 1 of 5)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 394,
                y: 376,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the next photo)'),
            targetPage.locator('div.slider-gallery button'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button)'),
            targetPage.locator(':scope >>> div.slider-gallery button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 1,
                y: 350,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the next photo)'),
            targetPage.locator('div.slider-gallery button'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button)'),
            targetPage.locator(':scope >>> div.slider-gallery button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 1,
                y: 350,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the next photo)'),
            targetPage.locator('div.slider-gallery button'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button)'),
            targetPage.locator(':scope >>> div.slider-gallery button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 1,
                y: 324,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the next photo)'),
            targetPage.locator('div.slider-gallery button'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button)'),
            targetPage.locator(':scope >>> div.slider-gallery button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 1,
                y: 324,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('button.slider-gallery-controls__control--next svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> button.slider-gallery-controls__control--next svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12,
                y: 27.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('button.slider-gallery-controls__control--next svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> button.slider-gallery-controls__control--next svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12,
                y: 27.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('button.slider-gallery-controls__control--prev svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> button.slider-gallery-controls__control--prev svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 4,
                y: 19.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('button.slider-gallery-controls__control--prev svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> button.slider-gallery-controls__control--prev svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 4,
                y: 19.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the next photo)'),
            targetPage.locator('button.slider-gallery-controls__control--next'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button[2])'),
            targetPage.locator(':scope >>> button.slider-gallery-controls__control--next')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3,
                y: 336,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the next photo)'),
            targetPage.locator('button.slider-gallery-controls__control--next'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button[2])'),
            targetPage.locator(':scope >>> button.slider-gallery-controls__control--next')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3,
                y: 336,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('button.slider-gallery-controls__control--next svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> button.slider-gallery-controls__control--next svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 6,
                y: 24.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('button.slider-gallery-controls__control--next svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> button.slider-gallery-controls__control--next svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 6,
                y: 24.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Shaun’s photo, 5 of 5)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 119,
                y: 135,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Shaun’s photo, 5 of 5)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 119,
                y: 135,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/people-nearby/zAhMACjIxNjYyMzUyMTQIJFv5hQAAAAAgy3DgovOgES_KDtaJhusdROPM7ep-7tKSUj1cqVt7YdI');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Select and send reaction)'),
            targetPage.locator("[data-qa='profile-card-action-reaction'] > button"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"profile-card-action-reaction\\"]/button)'),
            targetPage.locator(":scope >>> [data-qa='profile-card-action-reaction'] > button")
        ])
            .setTimeout(timeout)
            .click({
              delay: 500.90000000037253,
              offset: {
                x: 38.5,
                y: 46.8046875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Select and send reaction)'),
            targetPage.locator("[data-qa='profile-card-action-reaction'] > button"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"profile-card-action-reaction\\"]/button)'),
            targetPage.locator(":scope >>> [data-qa='profile-card-action-reaction'] > button")
        ])
            .setTimeout(timeout)
            .click({
              delay: 500.90000000037253,
              offset: {
                x: 38.5,
                y: 46.8046875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(😍[role=\\"image\\"])'),
            targetPage.locator("button:nth-of-type(1) > [data-qa='emoji-text']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"emoji-text\\"])'),
            targetPage.locator(":scope >>> button:nth-of-type(1) > [data-qa='emoji-text']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 11.5,
                y: 15.8046875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(😍[role=\\"image\\"])'),
            targetPage.locator("button:nth-of-type(1) > [data-qa='emoji-text']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"emoji-text\\"])'),
            targetPage.locator(":scope >>> button:nth-of-type(1) > [data-qa='emoji-text']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 11.5,
                y: 15.8046875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 39.0013427734375,
                y: 42.76611328125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 39.0013427734375,
                y: 42.76611328125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.modal-container > div > div > div > button'),
            targetPage.locator('::-p-xpath(//*[@id=\\"app-root\\"]/div/div[4]/div/div/div/button)'),
            targetPage.locator(':scope >>> div.modal-container > div > div > div > button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 154,
                y: 194,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.modal-container > div > div > div > button'),
            targetPage.locator('::-p-xpath(//*[@id=\\"app-root\\"]/div/div[4]/div/div/div/button)'),
            targetPage.locator(':scope >>> div.modal-container > div > div > div > button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 154,
                y: 194,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/own-profile');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Zack, 28 years old) >>>> ::-p-aria([role=\\"image\\"])'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(3) > div.people-nearby__content > [data-qa='people-nearby-content'] li:nth-of-type(6) [data-qa='avatar__image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"avatar__image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(3) > div.people-nearby__content > [data-qa='people-nearby-content'] li:nth-of-type(6) [data-qa='avatar__image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 37.671875,
                y: 129.625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Zack, 28 years old) >>>> ::-p-aria([role=\\"image\\"])'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(3) > div.people-nearby__content > [data-qa='people-nearby-content'] li:nth-of-type(6) [data-qa='avatar__image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"avatar__image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(3) > div.people-nearby__content > [data-qa='people-nearby-content'] li:nth-of-type(6) [data-qa='avatar__image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 37.671875,
                y: 129.625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Select and send reaction)'),
            targetPage.locator("[data-qa='profile-card-action-reaction'] > button"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"profile-card-action-reaction\\"]/button)'),
            targetPage.locator(":scope >>> [data-qa='profile-card-action-reaction'] > button")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 15.5,
                y: 41.8046875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Select and send reaction)'),
            targetPage.locator("[data-qa='profile-card-action-reaction'] > button"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"profile-card-action-reaction\\"]/button)'),
            targetPage.locator(":scope >>> [data-qa='profile-card-action-reaction'] > button")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 15.5,
                y: 41.8046875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(😍[role=\\"image\\"])'),
            targetPage.locator("button:nth-of-type(1) > [data-qa='emoji-text']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"emoji-text\\"])'),
            targetPage.locator(":scope >>> button:nth-of-type(1) > [data-qa='emoji-text']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 21.5,
                y: 3.8046875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(😍[role=\\"image\\"])'),
            targetPage.locator("button:nth-of-type(1) > [data-qa='emoji-text']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"emoji-text\\"])'),
            targetPage.locator(":scope >>> button:nth-of-type(1) > [data-qa='emoji-text']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 21.5,
                y: 3.8046875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.modal-container > div > div > div > button'),
            targetPage.locator('::-p-xpath(//*[@id=\\"app-root\\"]/div/div[4]/div/div/div/button)'),
            targetPage.locator(':scope >>> div.modal-container > div > div > div > button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 151,
                y: 68,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.modal-container > div > div > div > button'),
            targetPage.locator('::-p-xpath(//*[@id=\\"app-root\\"]/div/div[4]/div/div/div/button)'),
            targetPage.locator(':scope >>> div.modal-container > div > div > div > button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 151,
                y: 68,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/own-profile');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Viktor, 38 years old) >>>> ::-p-aria([role=\\"image\\"])'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(3) > div.people-nearby__content > [data-qa='people-nearby-content'] li:nth-of-type(12) [data-qa='avatar__image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"avatar__image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(3) > div.people-nearby__content > [data-qa='people-nearby-content'] li:nth-of-type(12) [data-qa='avatar__image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 47.671875,
                y: 136.375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Viktor, 38 years old) >>>> ::-p-aria([role=\\"image\\"])'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(3) > div.people-nearby__content > [data-qa='people-nearby-content'] li:nth-of-type(12) [data-qa='avatar__image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"avatar__image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(3) > div.people-nearby__content > [data-qa='people-nearby-content'] li:nth-of-type(12) [data-qa='avatar__image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 47.671875,
                y: 136.375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 20.166656494140625,
                y: 14.471710205078125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 20.166656494140625,
                y: 14.471710205078125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.modal-container > div > div > div > button'),
            targetPage.locator('::-p-xpath(//*[@id=\\"app-root\\"]/div/div[4]/div/div/div/button)'),
            targetPage.locator(':scope >>> div.modal-container > div > div > div > button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 136,
                y: 202,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.modal-container > div > div > div > button'),
            targetPage.locator('::-p-xpath(//*[@id=\\"app-root\\"]/div/div[4]/div/div/div/button)'),
            targetPage.locator(':scope >>> div.modal-container > div > div > div > button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 136,
                y: 202,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/own-profile');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='liked-you'] > span.csms-tabbar__label"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"liked-you\\"]/span[2])'),
            targetPage.locator(":scope >>> [data-qa='liked-you'] > span.csms-tabbar__label"),
            targetPage.locator('::-p-text(Likes)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 15.5625,
                y: 8,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='liked-you'] > span.csms-tabbar__label"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"liked-you\\"]/span[2])'),
            targetPage.locator(":scope >>> [data-qa='liked-you'] > span.csms-tabbar__label"),
            targetPage.locator('::-p-text(Likes)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 15.5625,
                y: 8,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div:nth-of-type(8) span.csms-user-card-content__slots'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/button/span[2])'),
            targetPage.locator(':scope >>> div:nth-of-type(8) span.csms-user-card-content__slots')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 55.5,
                y: 213.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div:nth-of-type(8) span.csms-user-card-content__slots'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/button/span[2])'),
            targetPage.locator(':scope >>> div:nth-of-type(8) span.csms-user-card-content__slots')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 55.5,
                y: 213.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 206.5,
                y: 394,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 206.5,
                y: 394,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(InkedD’s photo, 1 of 3)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 426,
                y: 481,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(InkedD’s photo, 1 of 3)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 426,
                y: 481,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/people-nearby');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(99+ Likes) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='liked-you'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='liked-you'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8.500244140625,
                y: 12.0025634765625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(99+ Likes) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='liked-you'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='liked-you'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8.500244140625,
                y: 12.0025634765625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div:nth-of-type(11) span.csms-user-card-content__slots'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/button/span[2])'),
            targetPage.locator(':scope >>> div:nth-of-type(11) span.csms-user-card-content__slots')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 84.5,
                y: 218.578125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div:nth-of-type(11) span.csms-user-card-content__slots'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/button/span[2])'),
            targetPage.locator(':scope >>> div:nth-of-type(11) span.csms-user-card-content__slots')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 84.5,
                y: 218.578125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 155.5,
                y: 251,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 155.5,
                y: 251,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Mohanad’s photo, 1 of 5)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 206,
                y: 279,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Mohanad’s photo, 1 of 5)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 206,
                y: 279,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Mohanad’s photo, 1 of 5)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 61,
                y: 98,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Mohanad’s photo, 1 of 5)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 61,
                y: 98,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/people-nearby');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(99+ Likes) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='liked-you'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='liked-you'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 4.500244140625,
                y: 2.0025634765625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(99+ Likes) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='liked-you'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='liked-you'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 4.500244140625,
                y: 2.0025634765625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div:nth-of-type(11) span.csms-user-card-content__slots'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/button/span[2])'),
            targetPage.locator(':scope >>> div:nth-of-type(11) span.csms-user-card-content__slots')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 94.5,
                y: 186.578125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div:nth-of-type(11) span.csms-user-card-content__slots'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/button/span[2])'),
            targetPage.locator(':scope >>> div:nth-of-type(11) span.csms-user-card-content__slots')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 94.5,
                y: 186.578125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('ArrowDown');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('ArrowDown');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('ArrowDown');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('ArrowDown');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('ArrowDown');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('ArrowDown');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('ArrowDown');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('ArrowDown');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('ArrowDown');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('ArrowDown');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('ArrowDown');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('ArrowDown');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.profile-card-full__menu svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> div.profile-card-full__menu svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8.5,
                y: 19,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.profile-card-full__menu svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> div.profile-card-full__menu svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8.5,
                y: 19,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.modal-container > div:nth-of-type(2) > div > div > button'),
            targetPage.locator('::-p-xpath(//*[@id=\\"app-root\\"]/div/div[4]/div[2]/div/div/button)'),
            targetPage.locator(':scope >>> div.modal-container > div:nth-of-type(2) > div > div > button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 372,
                y: 319,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.modal-container > div:nth-of-type(2) > div > div > button'),
            targetPage.locator('::-p-xpath(//*[@id=\\"app-root\\"]/div/div[4]/div[2]/div/div/button)'),
            targetPage.locator(':scope >>> div.modal-container > div:nth-of-type(2) > div > div > button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 372,
                y: 319,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 125.5,
                y: 202,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 125.5,
                y: 202,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Mohanad’s photo, 1 of 5)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 342,
                y: 327,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Mohanad’s photo, 1 of 5)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 342,
                y: 327,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Mohanad’s photo, 1 of 5)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 818,
                y: 330,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Mohanad’s photo, 1 of 5)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 818,
                y: 330,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.slider-gallery svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> div.slider-gallery svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 0,
                y: 9.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.slider-gallery svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> div.slider-gallery svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 0,
                y: 9.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the next photo)'),
            targetPage.locator('button.slider-gallery-controls__control--next'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button[2])'),
            targetPage.locator(':scope >>> button.slider-gallery-controls__control--next')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 7,
                y: 337,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the next photo)'),
            targetPage.locator('button.slider-gallery-controls__control--next'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button[2])'),
            targetPage.locator(':scope >>> button.slider-gallery-controls__control--next')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 7,
                y: 337,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the next photo)'),
            targetPage.locator('button.slider-gallery-controls__control--next'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button[2])'),
            targetPage.locator(':scope >>> button.slider-gallery-controls__control--next')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 7,
                y: 357,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the next photo)'),
            targetPage.locator('button.slider-gallery-controls__control--next'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button[2])'),
            targetPage.locator(':scope >>> button.slider-gallery-controls__control--next')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 7,
                y: 357,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('button.slider-gallery-controls__control--next svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> button.slider-gallery-controls__control--next svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12,
                y: 28.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('button.slider-gallery-controls__control--next svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> button.slider-gallery-controls__control--next svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12,
                y: 28.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Mohanad’s photo, 5 of 5)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 636,
                y: 35,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Mohanad’s photo, 5 of 5)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 636,
                y: 35,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Mohanad’s photo, 5 of 5)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 632,
                y: 24,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Mohanad’s photo, 5 of 5)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 632,
                y: 24,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Mohanad’s photo, 5 of 5)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 592,
                y: 37,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Mohanad’s photo, 5 of 5)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 592,
                y: 37,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Mohanad’s photo, 5 of 5)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 599,
                y: 23,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Mohanad’s photo, 5 of 5)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 599,
                y: 23,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Mohanad’s photo, 5 of 5)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 633,
                y: 36,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Mohanad’s photo, 5 of 5)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 633,
                y: 36,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Mohanad’s photo, 5 of 5)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 648,
                y: 36,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Mohanad’s photo, 5 of 5)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 648,
                y: 36,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/people-nearby');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='liked-you'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='liked-you'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 15.5,
                y: 3,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='liked-you'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='liked-you'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 15.5,
                y: 3,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div:nth-of-type(11) span.csms-user-card-content__slots'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/button/span[2])'),
            targetPage.locator(':scope >>> div:nth-of-type(11) span.csms-user-card-content__slots')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 119.5,
                y: 177.078125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div:nth-of-type(11) span.csms-user-card-content__slots'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/button/span[2])'),
            targetPage.locator(':scope >>> div:nth-of-type(11) span.csms-user-card-content__slots')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 119.5,
                y: 177.078125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('path:nth-of-type(3)'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(':scope >>> path:nth-of-type(3)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 11.166656494140625,
                y: 4.377288818359375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('path:nth-of-type(3)'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(':scope >>> path:nth-of-type(3)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 11.166656494140625,
                y: 4.377288818359375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Say Hello, Mohanad) >>>> ::-p-aria([role=\\"generic\\"])'),
            targetPage.locator('div:nth-of-type(11) div.csms-user-card-content__actions span'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/div[2]/button/span)'),
            targetPage.locator(':scope >>> div:nth-of-type(11) div.csms-user-card-content__actions span'),
            targetPage.locator('::-p-text(Say Hello)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 23.8203125,
                y: 9.2890625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Say Hello, Mohanad) >>>> ::-p-aria([role=\\"generic\\"])'),
            targetPage.locator('div:nth-of-type(11) div.csms-user-card-content__actions span'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/div[2]/button/span)'),
            targetPage.locator(':scope >>> div:nth-of-type(11) div.csms-user-card-content__actions span'),
            targetPage.locator('::-p-text(Say Hello)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 23.8203125,
                y: 9.2890625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-footer'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen-footer\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='screen-footer'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 126.5,
                y: 247.390625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-footer'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen-footer\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='screen-footer'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 126.5,
                y: 247.390625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('hi');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-primary'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3.5,
                y: 23,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-primary'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3.5,
                y: 23,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/liked-you');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='my-profile'] > span.csms-tabbar__label"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"my-profile\\"]/span[2])'),
            targetPage.locator(":scope >>> [data-qa='my-profile'] > span.csms-tabbar__label"),
            targetPage.locator('::-p-text(Profile)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 2.5859375,
                y: 7,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='my-profile'] > span.csms-tabbar__label"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"my-profile\\"]/span[2])'),
            targetPage.locator(":scope >>> [data-qa='my-profile'] > span.csms-tabbar__label"),
            targetPage.locator('::-p-text(Profile)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 2.5859375,
                y: 7,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Skip onboarding)'),
            targetPage.locator('div.csms-screen__block--align-stretch > div > button'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen\\"]/div[2]/div/button)'),
            targetPage.locator(':scope >>> div.csms-screen__block--align-stretch > div > button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 185,
                y: 244,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Skip onboarding)'),
            targetPage.locator('div.csms-screen__block--align-stretch > div > button'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen\\"]/div[2]/div/button)'),
            targetPage.locator(':scope >>> div.csms-screen__block--align-stretch > div > button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 185,
                y: 244,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Ash’s photo, 3 of 3[role=\\"image\\"])'),
            targetPage.locator("div:nth-of-type(3) [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> div:nth-of-type(3) [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 13.5,
                y: 67,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Ash’s photo, 3 of 3[role=\\"image\\"])'),
            targetPage.locator("div:nth-of-type(3) [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> div:nth-of-type(3) [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 13.5,
                y: 67,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 234,
                y: 317,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 234,
                y: 317,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the previous photo)'),
            targetPage.locator('div.slider-gallery button'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button)'),
            targetPage.locator(':scope >>> div.slider-gallery button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 7,
                y: 339,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the previous photo)'),
            targetPage.locator('div.slider-gallery button'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button)'),
            targetPage.locator(':scope >>> div.slider-gallery button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 7,
                y: 339,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('button.slider-gallery-controls__control--prev svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> button.slider-gallery-controls__control--prev svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8,
                y: 19.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('button.slider-gallery-controls__control--prev svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> button.slider-gallery-controls__control--prev svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8,
                y: 19.5,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/connections');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Ash , YOUR MOVE)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-text(AshAsh,YOUR MOVE)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 53.5,
                y: 33.0078125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Ash , YOUR MOVE)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-text(AshAsh,YOUR MOVE)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 53.5,
                y: 33.0078125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-footer'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen-footer\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='screen-footer'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 157.5,
                y: 197.390625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-footer'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen-footer\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='screen-footer'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 157.5,
                y: 197.390625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 121.5,
                y: 1,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 121.5,
                y: 1,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 109.5,
                y: 18,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 109.5,
                y: 18,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 152.5,
                y: 13,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 152.5,
                y: 13,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 142.5,
                y: 15,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 142.5,
                y: 15,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 64.5,
                y: 26,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 64.5,
                y: 26,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              count: 2,
              offset: {
                x: 59.5,
                y: 3,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              count: 2,
              offset: {
                x: 59.5,
                y: 3,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('he');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('hez');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('z');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('hez ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('hez');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Backspace');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('he');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Backspace');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('hey');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('y');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('hey ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('hey a');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('a');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('hey ag');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('g');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('hey aga');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('a');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('hey agai');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('hey agai ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('hey agai');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Backspace');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('hey again');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-primary'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12.5,
                y: 23,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-primary'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12.5,
                y: 23,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/connections');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='connections'] div.csms-dot-counter-notification__dot"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"dot-counter-notification\\"]/div[1])'),
            targetPage.locator(":scope >>> [data-qa='connections'] div.csms-dot-counter-notification__dot")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3.5,
                y: 4,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='connections'] div.csms-dot-counter-notification__dot"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"dot-counter-notification\\"]/div[1])'),
            targetPage.locator(":scope >>> [data-qa='connections'] div.csms-dot-counter-notification__dot")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3.5,
                y: 4,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(0 Chats) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='connections'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='connections'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 5.528564453125,
                y: 14.02752685546875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(0 Chats) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='connections'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='connections'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 5.528564453125,
                y: 14.02752685546875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(99+ Likes) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='liked-you'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='liked-you'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 13.500244140625,
                y: 13.0025634765625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(99+ Likes) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='liked-you'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='liked-you'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 13.500244140625,
                y: 13.0025634765625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div:nth-of-type(26) span.csms-user-card-content__slots'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/button/span[2])'),
            targetPage.locator(':scope >>> div:nth-of-type(26) span.csms-user-card-content__slots')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 74.5,
                y: 196.4296875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div:nth-of-type(26) span.csms-user-card-content__slots'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/button/span[2])'),
            targetPage.locator(':scope >>> div:nth-of-type(26) span.csms-user-card-content__slots')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 74.5,
                y: 196.4296875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 87.5,
                y: 288,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 87.5,
                y: 288,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.slider-gallery svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> div.slider-gallery svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 4,
                y: 19.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.slider-gallery svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> div.slider-gallery svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 4,
                y: 19.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.slider-gallery svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> div.slider-gallery svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8,
                y: 24.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.slider-gallery svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> div.slider-gallery svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8,
                y: 24.5,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/connections');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(99+ Likes) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='liked-you'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='liked-you'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 7.500244140625,
                y: 4.0025634765625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(99+ Likes) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='liked-you'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='liked-you'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 7.500244140625,
                y: 4.0025634765625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Like InkedD back)'),
            targetPage.locator('#page-container div:nth-of-type(8) button:nth-of-type(2)'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/div/button[2])'),
            targetPage.locator(':scope >>> #page-container div:nth-of-type(8) button:nth-of-type(2)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 49,
                y: 25.2421875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Like InkedD back)'),
            targetPage.locator('#page-container div:nth-of-type(8) button:nth-of-type(2)'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/div/button[2])'),
            targetPage.locator(':scope >>> #page-container div:nth-of-type(8) button:nth-of-type(2)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 49,
                y: 25.2421875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Say Hello, InkedD) >>>> ::-p-aria([role=\\"generic\\"])'),
            targetPage.locator('div:nth-of-type(8) div.csms-user-card-content__actions span'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/div[2]/button/span)'),
            targetPage.locator(':scope >>> div:nth-of-type(8) div.csms-user-card-content__actions span'),
            targetPage.locator('::-p-text(Say Hello)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 9.8203125,
                y: 11.1171875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Say Hello, InkedD) >>>> ::-p-aria([role=\\"generic\\"])'),
            targetPage.locator('div:nth-of-type(8) div.csms-user-card-content__actions span'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/div[2]/button/span)'),
            targetPage.locator(':scope >>> div:nth-of-type(8) div.csms-user-card-content__actions span'),
            targetPage.locator('::-p-text(Say Hello)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 9.8203125,
                y: 11.1171875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-footer'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen-footer\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='screen-footer'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 313.5,
                y: 238.390625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-footer'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen-footer\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='screen-footer'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 313.5,
                y: 238.390625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 203.5,
                y: 17,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 203.5,
                y: 17,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 62.5,
                y: 26,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 62.5,
                y: 26,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('hi');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-primary'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 19.5,
                y: 7,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-primary'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 19.5,
                y: 7,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/liked-you');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div:nth-of-type(16) span.csms-user-card-content__slots'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/button/span[2])'),
            targetPage.locator(':scope >>> div:nth-of-type(16) span.csms-user-card-content__slots')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 43.5,
                y: 197.75,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div:nth-of-type(16) span.csms-user-card-content__slots'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/button/span[2])'),
            targetPage.locator(':scope >>> div:nth-of-type(16) span.csms-user-card-content__slots')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 43.5,
                y: 197.75,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 251.5,
                y: 263,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 251.5,
                y: 263,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.slider-gallery svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> div.slider-gallery svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3,
                y: 16.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.slider-gallery svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> div.slider-gallery svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3,
                y: 16.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.slider-gallery svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> div.slider-gallery svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8,
                y: 27.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.slider-gallery svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> div.slider-gallery svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8,
                y: 27.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Laurynas’s photo, 3 of 8)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 608,
                y: 118,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Laurynas’s photo, 3 of 8)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 608,
                y: 118,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/connections');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='my-profile'] > span.csms-tabbar__label"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"my-profile\\"]/span[2])'),
            targetPage.locator(":scope >>> [data-qa='my-profile'] > span.csms-tabbar__label"),
            targetPage.locator('::-p-text(Profile)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 9.5859375,
                y: 9,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='my-profile'] > span.csms-tabbar__label"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"my-profile\\"]/span[2])'),
            targetPage.locator(":scope >>> [data-qa='my-profile'] > span.csms-tabbar__label"),
            targetPage.locator('::-p-text(Profile)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 9.5859375,
                y: 9,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Encounters)'),
            targetPage.locator("[data-qa='encounters']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"encounters\\"])'),
            targetPage.locator(":scope >>> [data-qa='encounters']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 45.5,
                y: 29,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Encounters)'),
            targetPage.locator("[data-qa='encounters']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"encounters\\"])'),
            targetPage.locator(":scope >>> [data-qa='encounters']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 45.5,
                y: 29,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 14.0013427734375,
                y: 56.34527587890625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 14.0013427734375,
                y: 56.34527587890625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 25.0013427734375,
                y: 48.14837646484375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 25.0013427734375,
                y: 48.14837646484375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Let’s Go)'),
            targetPage.locator("[data-qa='continue-button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"continue-button\\"])'),
            targetPage.locator(":scope >>> [data-qa='continue-button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 155.5,
                y: 27.90625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Let’s Go)'),
            targetPage.locator("[data-qa='continue-button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"continue-button\\"])'),
            targetPage.locator(":scope >>> [data-qa='continue-button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 155.5,
                y: 27.90625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('button:nth-of-type(2) path:nth-of-type(2)'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(':scope >>> button:nth-of-type(2) path:nth-of-type(2)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 21.1259765625,
                y: 27.6259765625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('button:nth-of-type(2) path:nth-of-type(2)'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(':scope >>> button:nth-of-type(2) path:nth-of-type(2)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 21.1259765625,
                y: 27.6259765625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('button:nth-of-type(2) path:nth-of-type(3)'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(':scope >>> button:nth-of-type(2) path:nth-of-type(3)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 1.5003662109375,
                y: 15.9774169921875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('button:nth-of-type(2) path:nth-of-type(3)'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(':scope >>> button:nth-of-type(2) path:nth-of-type(3)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 1.5003662109375,
                y: 15.9774169921875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('button:nth-of-type(2) path:nth-of-type(2)'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(':scope >>> button:nth-of-type(2) path:nth-of-type(2)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 11.1259765625,
                y: 16.6259765625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('button:nth-of-type(2) path:nth-of-type(2)'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(':scope >>> button:nth-of-type(2) path:nth-of-type(2)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 11.1259765625,
                y: 16.6259765625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Start Swiping)'),
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > [data-qa='fsw-carousel'] > div > div:nth-of-type(5) > div > [data-qa='cta-box'] > [data-qa='cta-box-buttons'] > [data-qa='csms-stack'] > [data-qa='csms-stack-item'] > [data-qa='csms-stack'] > [data-qa='csms-stack-item'] > [data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > [data-qa='fsw-carousel'] > div > div:nth-of-type(5) > div > [data-qa='cta-box'] > [data-qa='cta-box-buttons'] > [data-qa='csms-stack'] > [data-qa='csms-stack-item'] > [data-qa='csms-stack'] > [data-qa='csms-stack-item'] > [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 179.5,
                y: 12.796875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Start Swiping)'),
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > [data-qa='fsw-carousel'] > div > div:nth-of-type(5) > div > [data-qa='cta-box'] > [data-qa='cta-box-buttons'] > [data-qa='csms-stack'] > [data-qa='csms-stack-item'] > [data-qa='csms-stack'] > [data-qa='csms-stack-item'] > [data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > [data-qa='fsw-carousel'] > div > div:nth-of-type(5) > div > [data-qa='cta-box'] > [data-qa='cta-box-buttons'] > [data-qa='csms-stack'] > [data-qa='csms-stack-item'] > [data-qa='csms-stack'] > [data-qa='csms-stack-item'] > [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 179.5,
                y: 12.796875,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='first-card'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='first-card'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 55.5,
                y: 293,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='first-card'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='first-card'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 55.5,
                y: 293,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Alexcho’s photo, 1 of 2)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 355,
                y: 428,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Alexcho’s photo, 1 of 2)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 355,
                y: 428,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Alexcho’s photo, 1 of 2)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 789,
                y: 364,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Alexcho’s photo, 1 of 2)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 789,
                y: 364,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.slider-gallery svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> div.slider-gallery svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 21,
                y: 10.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.slider-gallery svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> div.slider-gallery svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 21,
                y: 10.5,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 10.166656494140625,
                y: 12.11639404296875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 10.166656494140625,
                y: 12.11639404296875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 7.166656494140625,
                y: 8.11639404296875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 7.166656494140625,
                y: 8.11639404296875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 15.0013427734375,
                y: 32.950714111328125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 15.0013427734375,
                y: 32.950714111328125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 20.0013427734375,
                y: 40.950714111328125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 20.0013427734375,
                y: 40.950714111328125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 97,
                y: 21.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 97,
                y: 21.3984375,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 17.0013427734375,
                y: 44.14801025390625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 17.0013427734375,
                y: 44.14801025390625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8.166656494140625,
                y: 11.976593017578125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8.166656494140625,
                y: 11.976593017578125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 1.166656494140625,
                y: 11.976806640625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 1.166656494140625,
                y: 11.976806640625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 112,
                y: 20.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 112,
                y: 20.3984375,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12.0013427734375,
                y: 44.1195068359375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12.0013427734375,
                y: 44.1195068359375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 93,
                y: 19.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 93,
                y: 19.3984375,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 23.0013427734375,
                y: 43.698211669921875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 23.0013427734375,
                y: 43.698211669921875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 2.166656494140625,
                y: 16.313720703125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 2.166656494140625,
                y: 16.313720703125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 5.53271484375,
                y: 6.90655517578125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 5.53271484375,
                y: 6.90655517578125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12.53271484375,
                y: 11.6636962890625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12.53271484375,
                y: 11.6636962890625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 15.53271484375,
                y: 14.64849853515625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 15.53271484375,
                y: 14.64849853515625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 13.0013427734375,
                y: 34.6925048828125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 13.0013427734375,
                y: 34.6925048828125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 20.0013427734375,
                y: 42.951263427734375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 20.0013427734375,
                y: 42.951263427734375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 22.0013427734375,
                y: 42.950164794921875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 22.0013427734375,
                y: 42.950164794921875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 6.166656494140625,
                y: 11.857574462890625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 6.166656494140625,
                y: 11.857574462890625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 26.0013427734375,
                y: 49.950439453125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 26.0013427734375,
                y: 49.950439453125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 22.166656494140625,
                y: 16.12872314453125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 22.166656494140625,
                y: 16.12872314453125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 113,
                y: 10.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 113,
                y: 10.3984375,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8.0013427734375,
                y: 40.69219970703125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8.0013427734375,
                y: 40.69219970703125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Close)'),
            targetPage.locator("[data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 0.5,
                y: 20,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Close)'),
            targetPage.locator("[data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 0.5,
                y: 20,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 9.166656494140625,
                y: 7.9677734375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 9.166656494140625,
                y: 7.9677734375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Close)'),
            targetPage.locator("[data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 5.5,
                y: 34,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Close)'),
            targetPage.locator("[data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 5.5,
                y: 34,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 50.0013427734375,
                y: 30.699127197265625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 50.0013427734375,
                y: 30.699127197265625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 14.166656494140625,
                y: 6.858184814453125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 14.166656494140625,
                y: 6.858184814453125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 78,
                y: 24.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 78,
                y: 24.3984375,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 22.0013427734375,
                y: 47.011749267578125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 22.0013427734375,
                y: 47.011749267578125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 89,
                y: 37.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 89,
                y: 37.3984375,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 15.0013427734375,
                y: 34.96221923828125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 15.0013427734375,
                y: 34.96221923828125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 71,
                y: 32.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 71,
                y: 32.3984375,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 4.166656494140625,
                y: 19.8638916015625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 4.166656494140625,
                y: 19.8638916015625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 89,
                y: 22.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 89,
                y: 22.3984375,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 6.166656494140625,
                y: 16.314788818359375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 6.166656494140625,
                y: 16.314788818359375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 2.0013427734375,
                y: 28.96728515625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 2.0013427734375,
                y: 28.96728515625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 19.0013427734375,
                y: 45.75323486328125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 19.0013427734375,
                y: 45.75323486328125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 29.0013427734375,
                y: 56.81109619140625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 29.0013427734375,
                y: 56.81109619140625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 31.0013427734375,
                y: 45.8115234375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 31.0013427734375,
                y: 45.8115234375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3.166656494140625,
                y: 15.782684326171875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3.166656494140625,
                y: 15.782684326171875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 14.0013427734375,
                y: 39.939971923828125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 14.0013427734375,
                y: 39.939971923828125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 4.53271484375,
                y: 20.90606689453125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 4.53271484375,
                y: 20.90606689453125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 17.0013427734375,
                y: 25.1624755859375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 17.0013427734375,
                y: 25.1624755859375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 11.0013427734375,
                y: 34.14837646484375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 11.0013427734375,
                y: 34.14837646484375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 6.166656494140625,
                y: 20.13385009765625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 6.166656494140625,
                y: 20.13385009765625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 10.53271484375,
                y: 6.858978271484375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 10.53271484375,
                y: 6.858978271484375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 21.166656494140625,
                y: 12.8580322265625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 21.166656494140625,
                y: 12.8580322265625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 30.0013427734375,
                y: 49.1632080078125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 30.0013427734375,
                y: 49.1632080078125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 79,
                y: 26.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 79,
                y: 26.3984375,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 7.166656494140625,
                y: 7.7738037109375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 7.166656494140625,
                y: 7.7738037109375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 83,
                y: 35.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 83,
                y: 35.3984375,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 11.0013427734375,
                y: 29.692047119140625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 11.0013427734375,
                y: 29.692047119140625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Chat With A Match)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div > [data-qa='cta-box'] > [data-qa='cta-box-buttons'] > [data-qa='csms-stack'] > [data-qa='csms-stack-item'] > [data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div > [data-qa='cta-box'] > [data-qa='cta-box-buttons'] > [data-qa='csms-stack'] > [data-qa='csms-stack-item'] > [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 119.5,
                y: 33.3046875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Chat With A Match)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div > [data-qa='cta-box'] > [data-qa='cta-box-buttons'] > [data-qa='csms-stack'] > [data-qa='csms-stack-item'] > [data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div > [data-qa='cta-box'] > [data-qa='cta-box-buttons'] > [data-qa='csms-stack'] > [data-qa='csms-stack-item'] > [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 119.5,
                y: 33.3046875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(N, Online now)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 9.5,
                y: 25.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(N, Online now)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 9.5,
                y: 25.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.navigation-profile'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar\\"]/div/div[2])'),
            targetPage.locator(':scope >>> div.navigation-profile')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 2.5,
                y: 33.203125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.navigation-profile'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar\\"]/div/div[2])'),
            targetPage.locator(':scope >>> div.navigation-profile')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 2.5,
                y: 33.203125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(N, Online now!, View Profile N) >>>> ::-p-aria([role=\\"image\\"])'),
            targetPage.locator("[data-qa='avatar__image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"avatar__image\\"])'),
            targetPage.locator(":scope >>> [data-qa='avatar__image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 16.5,
                y: 23.0078125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(N, Online now!, View Profile N) >>>> ::-p-aria([role=\\"image\\"])'),
            targetPage.locator("[data-qa='avatar__image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"avatar__image\\"])'),
            targetPage.locator(":scope >>> [data-qa='avatar__image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 16.5,
                y: 23.0078125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 153.5,
                y: 313,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 153.5,
                y: 313,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 223.5,
                y: 301,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 223.5,
                y: 301,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.slider-gallery svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> div.slider-gallery svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3,
                y: 15.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.slider-gallery svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> div.slider-gallery svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3,
                y: 15.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator(`::-p-aria(N\\'s fullscreen gallery[role=\\"dialog\\"]) >>>> ::-p-aria(N’s photo, 2 of 5)`),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 827,
                y: 343,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator(`::-p-aria(N\\'s fullscreen gallery[role=\\"dialog\\"]) >>>> ::-p-aria(N’s photo, 2 of 5)`),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 827,
                y: 343,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the next photo)'),
            targetPage.locator('button.slider-gallery-controls__control--next'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button[2])'),
            targetPage.locator(':scope >>> button.slider-gallery-controls__control--next')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3,
                y: 310,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the next photo)'),
            targetPage.locator('button.slider-gallery-controls__control--next'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button[2])'),
            targetPage.locator(':scope >>> button.slider-gallery-controls__control--next')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3,
                y: 310,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the previous photo)'),
            targetPage.locator('button.slider-gallery-controls__control--prev'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button[1])'),
            targetPage.locator(':scope >>> button.slider-gallery-controls__control--prev')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 42,
                y: 130,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the previous photo)'),
            targetPage.locator('button.slider-gallery-controls__control--prev'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button[1])'),
            targetPage.locator(':scope >>> button.slider-gallery-controls__control--prev')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 42,
                y: 130,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator(`::-p-aria(N\\'s fullscreen gallery[role=\\"dialog\\"]) >>>> ::-p-aria(N’s photo, 2 of 5)`),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 135,
                y: 167,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator(`::-p-aria(N\\'s fullscreen gallery[role=\\"dialog\\"]) >>>> ::-p-aria(N’s photo, 2 of 5)`),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 135,
                y: 167,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator(`::-p-aria(N\\'s fullscreen gallery[role=\\"dialog\\"]) >>>> ::-p-aria(N’s photo, 2 of 5)`),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 568,
                y: 132,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator(`::-p-aria(N\\'s fullscreen gallery[role=\\"dialog\\"]) >>>> ::-p-aria(N’s photo, 2 of 5)`),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 568,
                y: 132,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator(`::-p-aria(N\\'s fullscreen gallery[role=\\"dialog\\"]) >>>> ::-p-aria(N’s photo, 2 of 5)`),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 721,
                y: 110,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator(`::-p-aria(N\\'s fullscreen gallery[role=\\"dialog\\"]) >>>> ::-p-aria(N’s photo, 2 of 5)`),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 721,
                y: 110,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/chat-profile/zAhMACjIyMzI5MDU4ODUIJFv5hQAAAAAgnxmGVD6xTrQK74GpR7dlxDuGvXrBtDfGBDgalAuQMKA');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.profile-card-full__bottom-line div:nth-of-type(2) path:nth-of-type(2)'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(':scope >>> div.profile-card-full__bottom-line div:nth-of-type(2) path:nth-of-type(2)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 16.0013427734375,
                y: 35.865478515625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.profile-card-full__bottom-line div:nth-of-type(2) path:nth-of-type(2)'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(':scope >>> div.profile-card-full__bottom-line div:nth-of-type(2) path:nth-of-type(2)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 16.0013427734375,
                y: 35.865478515625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-footer'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen-footer\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='screen-footer'] > div")
        ])
            .setTimeout(timeout)
            .click({
              count: 2,
              offset: {
                x: 180.5,
                y: 222.796875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-footer'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen-footer\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='screen-footer'] > div")
        ])
            .setTimeout(timeout)
            .click({
              count: 2,
              offset: {
                x: 180.5,
                y: 222.796875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 105.5,
                y: 11,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 105.5,
                y: 11,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('y');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('y');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('a');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i wo');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i wok');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('k');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i woke');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i woke ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i woke u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i woke up');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('p');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i woke up ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i woke up a');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('a');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i woke up an');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i woke up and');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('d');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i woke up and ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i woke up and c');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('c');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i woke up and co');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i woke up and cou');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i woke up and coul');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('l');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i woke up and could');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('d');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i woke up and couldn');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i woke up and couldnt');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i woke up and couldnt ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i woke up and couldnt s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i woke up and couldnt sl');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('l');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i woke up and couldnt sle');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i woke up and couldnt slee');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ya i woke up and couldnt sleep');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('p');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Back)'),
            targetPage.locator("[data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3.5,
                y: 32,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Back)'),
            targetPage.locator("[data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3.5,
                y: 32,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/chat-profile/zAhMACjIyMzI5MDU4ODUIJFv5hQAAAAAgnxmGVD6xTrQK74GpR7dlxDuGvXrBtDfGBDgalAuQMKA');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Back)'),
            targetPage.locator("[data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 4.5,
                y: 28,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Back)'),
            targetPage.locator("[data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 4.5,
                y: 28,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/connections');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Alexcho, Online now)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 6.5,
                y: 25.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Alexcho, Online now)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 6.5,
                y: 25.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Alexcho’s photo, 1 of 2[role=\\"image\\"])'),
            targetPage.locator("div.mini-profile__gallery > div > div > div > div:nth-of-type(1) [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> div.mini-profile__gallery > div > div > div > div:nth-of-type(1) [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 40.5,
                y: 85,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Alexcho’s photo, 1 of 2[role=\\"image\\"])'),
            targetPage.locator("div.mini-profile__gallery > div > div > div > div:nth-of-type(1) [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> div.mini-profile__gallery > div > div > div > div:nth-of-type(1) [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 40.5,
                y: 85,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the next photo)'),
            targetPage.locator('div.slider-gallery button'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button)'),
            targetPage.locator(':scope >>> div.slider-gallery button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 11,
                y: 354,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Go to the next photo)'),
            targetPage.locator('div.slider-gallery button'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"fullscreen-gallery\\"]/div[2]/div[2]/button)'),
            targetPage.locator(':scope >>> div.slider-gallery button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 11,
                y: 354,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 107,
                y: 123,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 107,
                y: 123,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/connections');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Alexcho, Online now , YOUR MOVE)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-text(AlexchoAlexcho, Online now,YOUR)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 33.5,
                y: 29.0078125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Alexcho, Online now , YOUR MOVE)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-text(AlexchoAlexcho, Online now,YOUR)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 33.5,
                y: 29.0078125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen\\"])'),
            targetPage.locator(":scope >>> [data-qa='screen']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 259.5,
                y: 567,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen\\"])'),
            targetPage.locator(":scope >>> [data-qa='screen']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 259.5,
                y: 567,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 109.5,
                y: 19,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 109.5,
                y: 19,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('f');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('f');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fi');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fin');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine th');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('x');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n u ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n u ,');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(',');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n u , ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n u , i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n u , im');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('m');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n u , im ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n u , im f');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('f');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n u , im fr');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('r');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n u , im fre');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n u , im fren');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n u , im frenc');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('c');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n u , im french');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n u , im french ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n u , im french s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n u , im french sw');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n u , im french swi');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n u , im french swis');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n u , im french swiss');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n u , im french swiss ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Back)'),
            targetPage.locator("[data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3.5,
                y: 14,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Back)'),
            targetPage.locator("[data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3.5,
                y: 14,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/connections');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Abdel, Online now)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12.5,
                y: 29.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Abdel, Online now)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12.5,
                y: 29.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen\\"])'),
            targetPage.locator(":scope >>> [data-qa='screen']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 87.5,
                y: 563,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen\\"])'),
            targetPage.locator(":scope >>> [data-qa='screen']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 87.5,
                y: 563,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('hi');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Back)'),
            targetPage.locator("[data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 2.5,
                y: 27,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Back)'),
            targetPage.locator("[data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 2.5,
                y: 27,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/connections');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Simon, Online now)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 11.5,
                y: 11.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Simon, Online now)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 11.5,
                y: 11.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen\\"])'),
            targetPage.locator(":scope >>> [data-qa='screen']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 149.5,
                y: 579,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen\\"])'),
            targetPage.locator(":scope >>> [data-qa='screen']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 149.5,
                y: 579,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('f');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('f');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fi');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fin');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine n ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine n u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Back)'),
            targetPage.locator("[data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 6.5,
                y: 14,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Back)'),
            targetPage.locator("[data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 6.5,
                y: 14,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/connections');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Profile)'),
            targetPage.locator("[data-qa='my-profile']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"my-profile\\"])'),
            targetPage.locator(":scope >>> [data-qa='my-profile']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 45.5,
                y: 21,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Profile)'),
            targetPage.locator("[data-qa='my-profile']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"my-profile\\"])'),
            targetPage.locator(":scope >>> [data-qa='my-profile']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 45.5,
                y: 21,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='profile-info__name'] > span"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"profile-info__name\\"]/span)'),
            targetPage.locator(":scope >>> [data-qa='profile-info__name'] > span")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 19.5,
                y: 20,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='profile-info__name'] > span"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"profile-info__name\\"]/span)'),
            targetPage.locator(":scope >>> [data-qa='profile-info__name'] > span")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 19.5,
                y: 20,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('circle.csms-progress-circle__svg-path-indicator'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"brick-action\\"]/span[2]/div/span/svg/circle[2])'),
            targetPage.locator(':scope >>> circle.csms-progress-circle__svg-path-indicator')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 59.445465087890625,
                y: 56.945457458496094,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('circle.csms-progress-circle__svg-path-indicator'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"brick-action\\"]/span[2]/div/span/svg/circle[2])'),
            targetPage.locator(':scope >>> circle.csms-progress-circle__svg-path-indicator')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 59.445465087890625,
                y: 56.945457458496094,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='profile-card-close'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='profile-card-close'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 10.5,
                y: 22,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='profile-card-close'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='profile-card-close'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 10.5,
                y: 22,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='profile-info__name'] > span"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"profile-info__name\\"]/span)'),
            targetPage.locator(":scope >>> [data-qa='profile-info__name'] > span")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 23.5,
                y: 16,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='profile-info__name'] > span"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"profile-info__name\\"]/span)'),
            targetPage.locator(":scope >>> [data-qa='profile-info__name'] > span")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 23.5,
                y: 16,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-header'] div:nth-of-type(2) svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='screen-header'] div:nth-of-type(2) svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 16.5,
                y: 21,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-header'] div:nth-of-type(2) svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='screen-header'] div:nth-of-type(2) svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 16.5,
                y: 21,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Back)'),
            targetPage.locator("[data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 5.5,
                y: 36,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Back)'),
            targetPage.locator("[data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 5.5,
                y: 36,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/own-profile');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(99+ Likes) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='liked-you'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='liked-you'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 1.500244140625,
                y: 7.0025634765625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(99+ Likes) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='liked-you'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='liked-you'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 1.500244140625,
                y: 7.0025634765625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(4 Chats)'),
            targetPage.locator("[data-qa='connections']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections\\"])'),
            targetPage.locator(":scope >>> [data-qa='connections']"),
            targetPage.locator('::-p-text(4Chats)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 47.5,
                y: 22,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(4 Chats)'),
            targetPage.locator("[data-qa='connections']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections\\"])'),
            targetPage.locator(":scope >>> [data-qa='connections']"),
            targetPage.locator('::-p-text(4Chats)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 47.5,
                y: 22,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(99+ Likes)'),
            targetPage.locator("[data-qa='liked-you']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"liked-you\\"])'),
            targetPage.locator(":scope >>> [data-qa='liked-you']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 44.5,
                y: 19,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(99+ Likes)'),
            targetPage.locator("[data-qa='liked-you']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"liked-you\\"])'),
            targetPage.locator(":scope >>> [data-qa='liked-you']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 44.5,
                y: 19,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#tabbar'),
            targetPage.locator('::-p-xpath(//*[@id=\\"tabbar\\"])'),
            targetPage.locator(':scope >>> #tabbar')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 565.5,
                y: 5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#tabbar'),
            targetPage.locator('::-p-xpath(//*[@id=\\"tabbar\\"])'),
            targetPage.locator(':scope >>> #tabbar')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 565.5,
                y: 5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Profile)'),
            targetPage.locator("[data-qa='my-profile']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"my-profile\\"])'),
            targetPage.locator(":scope >>> [data-qa='my-profile']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 39.5,
                y: 43,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Profile)'),
            targetPage.locator("[data-qa='my-profile']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"my-profile\\"])'),
            targetPage.locator(":scope >>> [data-qa='my-profile']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 39.5,
                y: 43,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='liked-you'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='liked-you'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 7.5,
                y: 21,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='liked-you'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='liked-you'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 7.5,
                y: 21,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/connections');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.csms-screen__block--align-stretch > div > div > div:nth-of-type(2) span.csms-user-card-content__slots'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/button/span[2])'),
            targetPage.locator(':scope >>> div.csms-screen__block--align-stretch > div > div > div:nth-of-type(2) span.csms-user-card-content__slots')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 66.5,
                y: 186.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.csms-screen__block--align-stretch > div > div > div:nth-of-type(2) span.csms-user-card-content__slots'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/button/span[2])'),
            targetPage.locator(':scope >>> div.csms-screen__block--align-stretch > div > div > div:nth-of-type(2) span.csms-user-card-content__slots')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 66.5,
                y: 186.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('path:nth-of-type(3)'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(':scope >>> path:nth-of-type(3)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 4.166656494140625,
                y: 19.1187744140625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('path:nth-of-type(3)'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(':scope >>> path:nth-of-type(3)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 4.166656494140625,
                y: 19.1187744140625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Say Hello, Sammy)'),
            targetPage.locator('div.csms-screen__block--align-stretch > div > div > div:nth-of-type(2) button'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/div[2]/button)'),
            targetPage.locator(':scope >>> div.csms-screen__block--align-stretch > div > div > div:nth-of-type(2) button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 123.5,
                y: 33.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Say Hello, Sammy)'),
            targetPage.locator('div.csms-screen__block--align-stretch > div > div > div:nth-of-type(2) button'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/div[2]/button)'),
            targetPage.locator(':scope >>> div.csms-screen__block--align-stretch > div > div > div:nth-of-type(2) button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 123.5,
                y: 33.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Say Hello)'),
            targetPage.locator("[data-qa='chat-nudge-primary-button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-nudge-primary-button\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-nudge-primary-button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 228.5,
                y: 32.1875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Say Hello)'),
            targetPage.locator("[data-qa='chat-nudge-primary-button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-nudge-primary-button\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-nudge-primary-button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 228.5,
                y: 32.1875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-primary'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3.5,
                y: 14,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-primary'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3.5,
                y: 14,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/liked-you');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Like Zakaria back) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator('div:nth-of-type(24) button:nth-of-type(2) path'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(':scope >>> div:nth-of-type(24) button:nth-of-type(2) path')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 6.33331298828125,
                y: 6.265106201171875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Like Zakaria back) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator('div:nth-of-type(24) button:nth-of-type(2) path'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(':scope >>> div:nth-of-type(24) button:nth-of-type(2) path')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 6.33331298828125,
                y: 6.265106201171875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Say Hello, Zakaria) >>>> ::-p-aria([role=\\"generic\\"])'),
            targetPage.locator('div:nth-of-type(24) div.csms-user-card-content__actions span'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/div[2]/button/span)'),
            targetPage.locator(':scope >>> div:nth-of-type(24) div.csms-user-card-content__actions span'),
            targetPage.locator('::-p-text(Say Hello)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 31.8203125,
                y: 10.8046875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Say Hello, Zakaria) >>>> ::-p-aria([role=\\"generic\\"])'),
            targetPage.locator('div:nth-of-type(24) div.csms-user-card-content__actions span'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/div[2]/button/span)'),
            targetPage.locator(':scope >>> div:nth-of-type(24) div.csms-user-card-content__actions span'),
            targetPage.locator('::-p-text(Say Hello)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 31.8203125,
                y: 10.8046875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Say Hello)'),
            targetPage.locator("[data-qa='chat-nudge-primary-button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-nudge-primary-button\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-nudge-primary-button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 200.5,
                y: 19.1875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Say Hello)'),
            targetPage.locator("[data-qa='chat-nudge-primary-button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-nudge-primary-button\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-nudge-primary-button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 200.5,
                y: 19.1875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Back)'),
            targetPage.locator("[data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 6.5,
                y: 13,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Back)'),
            targetPage.locator("[data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 6.5,
                y: 13,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/liked-you');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div:nth-of-type(31) span.csms-user-card-content__slots'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/button/span[2])'),
            targetPage.locator(':scope >>> div:nth-of-type(31) span.csms-user-card-content__slots')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 92.5,
                y: 179.4375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div:nth-of-type(31) span.csms-user-card-content__slots'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/button/span[2])'),
            targetPage.locator(':scope >>> div:nth-of-type(31) span.csms-user-card-content__slots')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 92.5,
                y: 179.4375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 93.5,
                y: 251,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > div > [data-qa='profile-portal-content-container_wrapper'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 93.5,
                y: 251,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.slider-gallery svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> div.slider-gallery svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 9,
                y: 13.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.slider-gallery svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> div.slider-gallery svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 9,
                y: 13.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Denis’s photo, 2 of 6)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 822,
                y: 361,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Denis’s photo, 2 of 6)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 822,
                y: 361,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Denis’s photo, 2 of 6)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 832,
                y: 311,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Denis’s photo, 2 of 6)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 832,
                y: 311,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/connections');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(99+ Likes)'),
            targetPage.locator("[data-qa='liked-you']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"liked-you\\"])'),
            targetPage.locator(":scope >>> [data-qa='liked-you']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 57.5,
                y: 33,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(99+ Likes)'),
            targetPage.locator("[data-qa='liked-you']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"liked-you\\"])'),
            targetPage.locator(":scope >>> [data-qa='liked-you']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 57.5,
                y: 33,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div:nth-of-type(24) button:nth-of-type(2) svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> div:nth-of-type(24) button:nth-of-type(2) svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 21.25,
                y: 14.4296875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div:nth-of-type(24) button:nth-of-type(2) svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> div:nth-of-type(24) button:nth-of-type(2) svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 21.25,
                y: 14.4296875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Say Hello, Soo) >>>> ::-p-aria([role=\\"generic\\"])'),
            targetPage.locator('div:nth-of-type(24) div.csms-user-card-content__actions span'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/div[2]/button/span)'),
            targetPage.locator(':scope >>> div:nth-of-type(24) div.csms-user-card-content__actions span'),
            targetPage.locator('::-p-text(Say Hello)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 28.8203125,
                y: 13.3046875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Say Hello, Soo) >>>> ::-p-aria([role=\\"generic\\"])'),
            targetPage.locator('div:nth-of-type(24) div.csms-user-card-content__actions span'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/div[2]/button/span)'),
            targetPage.locator(':scope >>> div:nth-of-type(24) div.csms-user-card-content__actions span'),
            targetPage.locator('::-p-text(Say Hello)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 28.8203125,
                y: 13.3046875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Say Hello)'),
            targetPage.locator("[data-qa='chat-nudge-primary-button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-nudge-primary-button\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-nudge-primary-button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 227.5,
                y: 39.1875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Say Hello)'),
            targetPage.locator("[data-qa='chat-nudge-primary-button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-nudge-primary-button\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-nudge-primary-button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 227.5,
                y: 39.1875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-primary'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 1.5,
                y: 6,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-primary'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 1.5,
                y: 6,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/liked-you');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#tabbar path:nth-of-type(2)'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(':scope >>> #tabbar path:nth-of-type(2)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8.49725341796875,
                y: 2.00054931640625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#tabbar path:nth-of-type(2)'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(':scope >>> #tabbar path:nth-of-type(2)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8.49725341796875,
                y: 2.00054931640625,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 14.166656494140625,
                y: 3.5721435546875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 14.166656494140625,
                y: 3.5721435546875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Close)'),
            targetPage.locator("[data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 6.5,
                y: 14,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Close)'),
            targetPage.locator("[data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 6.5,
                y: 14,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 4.166656494140625,
                y: 16.59222412109375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 4.166656494140625,
                y: 16.59222412109375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 117,
                y: 13.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 117,
                y: 13.3984375,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 13.166656494140625,
                y: 22.3140869140625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 13.166656494140625,
                y: 22.3140869140625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 131,
                y: 35.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 131,
                y: 35.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 30.0013427734375,
                y: 46.962493896484375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 30.0013427734375,
                y: 46.962493896484375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 125,
                y: 13.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 125,
                y: 13.3984375,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 11.0013427734375,
                y: 49.616607666015625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 11.0013427734375,
                y: 49.616607666015625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 103,
                y: 35.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 103,
                y: 35.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 5.53271484375,
                y: 14.231048583984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 5.53271484375,
                y: 14.231048583984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 9.0013427734375,
                y: 18.95098876953125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 9.0013427734375,
                y: 18.95098876953125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 105,
                y: 20.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 105,
                y: 20.3984375,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 5.166656494140625,
                y: 6.976806640625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 5.166656494140625,
                y: 6.976806640625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 99,
                y: 22.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 99,
                y: 22.3984375,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 22.0013427734375,
                y: 44.405975341796875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 22.0013427734375,
                y: 44.405975341796875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 82,
                y: 12.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 82,
                y: 12.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 11.0013427734375,
                y: 53.7320556640625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 11.0013427734375,
                y: 53.7320556640625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-header'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='screen-header'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 0.5,
                y: 5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-header'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='screen-header'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 0.5,
                y: 5,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 15.166656494140625,
                y: 10.802825927734375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 15.166656494140625,
                y: 10.802825927734375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 10.166656494140625,
                y: 21.959564208984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 10.166656494140625,
                y: 21.959564208984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 19.166656494140625,
                y: 19.31298828125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 19.166656494140625,
                y: 19.31298828125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 17.166656494140625,
                y: 0.116119384765625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 17.166656494140625,
                y: 0.116119384765625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8.166656494140625,
                y: 16.10565185546875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8.166656494140625,
                y: 16.10565185546875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 5.53271484375,
                y: 8.662933349609375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 5.53271484375,
                y: 8.662933349609375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 39.0013427734375,
                y: 18.9627685546875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 39.0013427734375,
                y: 18.9627685546875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 27.0013427734375,
                y: 44.950439453125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 27.0013427734375,
                y: 44.950439453125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8.166656494140625,
                y: 7.864349365234375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8.166656494140625,
                y: 7.864349365234375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(12 Chats) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='connections'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='connections'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 10.528564453125,
                y: 12.02752685546875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(12 Chats) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='connections'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='connections'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 10.528564453125,
                y: 12.02752685546875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Abdel, Online now)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 53.5,
                y: 34.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Abdel, Online now)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 53.5,
                y: 34.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-footer'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen-footer\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='screen-footer'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 168.5,
                y: 192.390625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-footer'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen-footer\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='screen-footer'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 168.5,
                y: 192.390625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 93.5,
                y: 20,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 93.5,
                y: 20,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('f');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('f');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fr');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('r');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fra');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('a');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fran');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('franc');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('c');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('france');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('body'),
            targetPage.locator('::-p-xpath(/html/body)'),
            targetPage.locator(':scope >>> body')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 102,
                y: 37,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('body'),
            targetPage.locator('::-p-xpath(/html/body)'),
            targetPage.locator(':scope >>> body')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 102,
                y: 37,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-primary'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 2.5,
                y: 21,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-primary'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 2.5,
                y: 21,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/connections');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Alexcho, Online now)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 27.5,
                y: 41.90625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Alexcho, Online now)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 27.5,
                y: 41.90625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Get Notified)'),
            targetPage.locator("[data-qa='chat-nudge-primary-button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-nudge-primary-button\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-nudge-primary-button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 351.5,
                y: 28.1875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Get Notified)'),
            targetPage.locator("[data-qa='chat-nudge-primary-button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-nudge-primary-button\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-nudge-primary-button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 351.5,
                y: 28.1875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen\\"])'),
            targetPage.locator(":scope >>> [data-qa='screen']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 351.5,
                y: 561,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen\\"])'),
            targetPage.locator(":scope >>> [data-qa='screen']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 351.5,
                y: 561,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ni');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nic');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('c');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i l');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('l');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i li');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i liv');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('v');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in g');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('g');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in ge');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in gen');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in gene');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in genev');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('v');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('a');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva b');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('b');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva ba');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('a');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva bab');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('b');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('y');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby .');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('.');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('.');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('r');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u m');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('m');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u me');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mea');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('a');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean wh');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean whe');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean wher');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('r');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where im');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('m');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where im ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where im i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where im i ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where im i s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where im i st');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where im i sta');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('a');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where im i stay');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('y');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where im i stayi');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where im i stayin');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where im i staying');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('g');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where im i staying ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where im i staying h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where im i staying he');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where im i staying her');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('r');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where im i staying here');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where im i staying here ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where im i staying here i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where im i staying here in');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where im i staying here in ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where im i staying here in l');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('l');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where im i staying here in lo');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where im i staying here in lon');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where im i staying here in lono');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where im i staying here in lonon');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('nice i live in geneva baby ..or u mean where im i staying here in lonon?');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Shift');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up("'");
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up("'");
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-primary'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3.5,
                y: 14,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-primary'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3.5,
                y: 14,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/connections');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Simon, Online now)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 25.5,
                y: 16.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Simon, Online now)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 25.5,
                y: 16.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-footer'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen-footer\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='screen-footer'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 77.5,
                y: 194.390625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-footer'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen-footer\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='screen-footer'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 77.5,
                y: 194.390625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('a');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('a');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('as');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('as ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('as i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('as if');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('f');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('as if ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('as if i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('as if it');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('as if it ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('as if it w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('as if it wa');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('a');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('as if it was');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('as if it wasn');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('as if it wasnt');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('as if it wasnt ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('as if it wasnt w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('as if it wasnt wr');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('r');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('as if it wasnt wri');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('as if it wasnt writ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('as if it wasnt writt');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('as if it wasnt writte');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('as if it wasnt written');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Back)'),
            targetPage.locator("[data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 9.5,
                y: 33,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Back)'),
            targetPage.locator("[data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 9.5,
                y: 33,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/connections');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(99+ Likes)'),
            targetPage.locator("[data-qa='liked-you']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"liked-you\\"])'),
            targetPage.locator(":scope >>> [data-qa='liked-you']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 47.5,
                y: 28,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(99+ Likes)'),
            targetPage.locator("[data-qa='liked-you']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"liked-you\\"])'),
            targetPage.locator(":scope >>> [data-qa='liked-you']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 47.5,
                y: 28,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(7 Chats)'),
            targetPage.locator("[data-qa='connections']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections\\"])'),
            targetPage.locator(":scope >>> [data-qa='connections']"),
            targetPage.locator('::-p-text(7Chats)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 83.5,
                y: 18,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(7 Chats)'),
            targetPage.locator("[data-qa='connections']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections\\"])'),
            targetPage.locator(":scope >>> [data-qa='connections']"),
            targetPage.locator('::-p-text(7Chats)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 83.5,
                y: 18,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Alexcho, Online now)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 125.5,
                y: 21.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Alexcho, Online now)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 125.5,
                y: 21.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('z');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('z');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('z');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('z');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('r');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('r');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('r');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('r');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen\\"])'),
            targetPage.locator(":scope >>> [data-qa='screen']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 313.5,
                y: 563,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen\\"])'),
            targetPage.locator(":scope >>> [data-qa='screen']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 313.5,
                y: 563,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 281.5,
                y: 18,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 281.5,
                y: 18,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('sw');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('swi');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('swit');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('switz');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('z');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('switze');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('switzer');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('r');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('switzerl');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('l');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('switzerla');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('a');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('switzerlan');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('switzerland');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('d');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('switzerland ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('switzerland b');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('b');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('switzerland ba');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('a');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('switzerland bab');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('b');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('switzerland baby');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('y');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('switzerland baby ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('switzerland baby !');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('!');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Shift');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-primary'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 10.5,
                y: 6,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-primary'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 10.5,
                y: 6,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/connections');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Profile)'),
            targetPage.locator("[data-qa='my-profile']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"my-profile\\"])'),
            targetPage.locator(":scope >>> [data-qa='my-profile']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 67.5,
                y: 32,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Profile)'),
            targetPage.locator("[data-qa='my-profile']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"my-profile\\"])'),
            targetPage.locator(":scope >>> [data-qa='my-profile']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 67.5,
                y: 32,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Settings) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='navbar-slot-secondary'] > div:nth-of-type(1) path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-secondary'] > div:nth-of-type(1) path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 5.498779296875,
                y: 17.998804092407227,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Settings) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='navbar-slot-secondary'] > div:nth-of-type(1) path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-secondary'] > div:nth-of-type(1) path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 5.498779296875,
                y: 17.998804092407227,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-header'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='screen-header'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8.5,
                y: 20,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-header'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='screen-header'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8.5,
                y: 20,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/own-profile');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Edit profile) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='screen-header'] div:nth-of-type(2) path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='screen-header'] div:nth-of-type(2) path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8.5,
                y: 12,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Edit profile) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='screen-header'] div:nth-of-type(2) path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='screen-header'] div:nth-of-type(2) path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8.5,
                y: 12,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('li:nth-of-type(3) svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> li:nth-of-type(3) svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 17.21875,
                y: 8.03125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('li:nth-of-type(3) svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> li:nth-of-type(3) svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 17.21875,
                y: 8.03125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Your Photos)'),
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > [data-qa='action-sheet'] > [data-qa='action-sheet-list'] > [data-qa='action-sheet-list-item'] > [data-qa='action-sheet-item']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"action-sheet-item\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > [data-qa='action-sheet'] > [data-qa='action-sheet-list'] > [data-qa='action-sheet-list-item'] > [data-qa='action-sheet-item']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 245.5,
                y: 14,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Your Photos)'),
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > [data-qa='action-sheet'] > [data-qa='action-sheet-list'] > [data-qa='action-sheet-list-item'] > [data-qa='action-sheet-item']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"action-sheet-item\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > [data-qa='action-sheet'] > [data-qa='action-sheet-list'] > [data-qa='action-sheet-list-item'] > [data-qa='action-sheet-item']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 245.5,
                y: 14,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Choose Files: IMG_20240311_174939_047.jpg)'),
            targetPage.locator('#file-picker'),
            targetPage.locator('::-p-xpath(//*[@id=\\"file-picker\\"])'),
            targetPage.locator(':scope >>> #file-picker')
        ])
            .setTimeout(timeout)
            .fill('C:\\fakepath\\IMG_20240311_174939_047.jpg');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria([role=\\"dialog\\"]) >>>> ::-p-aria([role=\\"image\\"])'),
            targetPage.locator("#js-fullscreen [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> #js-fullscreen [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 129.25,
                y: 101,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria([role=\\"dialog\\"]) >>>> ::-p-aria([role=\\"image\\"])'),
            targetPage.locator("#js-fullscreen [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> #js-fullscreen [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 129.25,
                y: 101,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Upload 1)'),
            targetPage.locator("#js-fullscreen [data-qa='navbar-link']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-link\\"])'),
            targetPage.locator(":scope >>> #js-fullscreen [data-qa='navbar-link']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 51.15625,
                y: 29,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Upload 1)'),
            targetPage.locator("#js-fullscreen [data-qa='navbar-link']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-link\\"])'),
            targetPage.locator(":scope >>> #js-fullscreen [data-qa='navbar-link']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 51.15625,
                y: 29,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/own-profile-edit');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Back) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='screen-header'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='screen-header'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 2.4995574951171875,
                y: 9.00006103515625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Back) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='screen-header'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='screen-header'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 2.4995574951171875,
                y: 9.00006103515625,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/own-profile');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='connections'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='connections'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 22.5,
                y: 20,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='connections'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='connections'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 22.5,
                y: 20,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='my-profile'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='my-profile'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3.5,
                y: 11,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='my-profile'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='my-profile'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3.5,
                y: 11,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='profile-info__name'] > span"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"profile-info__name\\"]/span)'),
            targetPage.locator(":scope >>> [data-qa='profile-info__name'] > span")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 44.5,
                y: 19,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='profile-info__name'] > span"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"profile-info__name\\"]/span)'),
            targetPage.locator(":scope >>> [data-qa='profile-info__name'] > span")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 44.5,
                y: 19,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Edit profile) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='screen-header'] div:nth-of-type(2) path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='screen-header'] div:nth-of-type(2) path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 0.5,
                y: 14,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Edit profile) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='screen-header'] div:nth-of-type(2) path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='screen-header'] div:nth-of-type(2) path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 0.5,
                y: 14,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-header'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='screen-header'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 5.5,
                y: 19,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-header'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='screen-header'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 5.5,
                y: 19,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/own-profile');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(9 Chats) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='connections'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='connections'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 1.528564453125,
                y: 6.02752685546875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(9 Chats) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='connections'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='connections'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 1.528564453125,
                y: 6.02752685546875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Simon, Online now)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 184.5,
                y: 37.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Simon, Online now)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 184.5,
                y: 37.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('k');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('k');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('k');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('k');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('g');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('g');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('g');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('g');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('l');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('l');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('l');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('l');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('d');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('d');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('d');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('d');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-footer'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen-footer\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='screen-footer'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 281.5,
                y: 206.390625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-footer'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen-footer\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='screen-footer'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 281.5,
                y: 206.390625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('k');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('k');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('kn');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('kno');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('know');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowi');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowin');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('g');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing ea');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('a');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing eac');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('c');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each ot');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each oth');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each othe');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('r');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other wo');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other wou');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other woul');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('l');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('d');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would b');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('b');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be th');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the d');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('d');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the de');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dea');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('a');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('d');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead en');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('d');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Backspace');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end wh');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end whe');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when m');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('m');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when me');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when mee');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meet');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeti');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meetin');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('g');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting st');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting str');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('r');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting stra');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('a');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting stran');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strang');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('g');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strange');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting stranger');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('r');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers f');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('f');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers fo');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('r');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for f');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('f');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fu');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is th');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the a');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('a');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the an');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the ans');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answe');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('r');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to y');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('y');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to yo');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to you');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('r');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your q');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('q');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Backspace');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your p');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('p');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your pr');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('r');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your pre');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your prev');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('v');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previo');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previou');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous q');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('q');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous qu');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous que');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous ques');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous quest');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous questi');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous questio');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question,');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(',');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, a');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('a');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as wr');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('r');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as wri');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as writ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as writt');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as writte');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in th');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the p');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('p');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the pr');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('r');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the pro');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the prof');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('f');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profi');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profil');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('l');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profi');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Backspace');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profil');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('l');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile b');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('b');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile ba');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('a');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile bab');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('b');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile baby');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('y');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile baby ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile baby .');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('.');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile baby ..');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('.');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile baby ..i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile baby ..i ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile baby ..i d');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('d');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile baby ..i do');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile baby ..i do ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile baby ..i do h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile baby ..i do ho');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile baby ..i do hop');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('p');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile baby ..i do hope');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile baby ..i do hope ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile baby ..i do hope u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile baby ..i do hope u ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile baby ..i do hope u r');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('r');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile baby ..i do hope u re');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile baby ..i do hope u rea');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('a');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile baby ..i do hope u read');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('d');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile baby ..i do hope u read ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile baby ..i do hope u read i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile baby ..i do hope u read it');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile baby ..i do hope u read it ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('knowing each other would be the dead end when meeting strangers for fun is the answer to your previous question, as written in the profile baby ..i do hope u read it !');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('!');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Shift');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Back) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='navbar-slot-primary'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8.499557495117188,
                y: 14.00006103515625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Back) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='navbar-slot-primary'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8.499557495117188,
                y: 14.00006103515625,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/connections');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Abdel, Online now)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 107.5,
                y: 20.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Abdel, Online now)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 107.5,
                y: 20.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-footer'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen-footer\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='screen-footer'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 389.5,
                y: 208.390625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-footer'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen-footer\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='screen-footer'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 389.5,
                y: 208.390625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('th');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('thx');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('x');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('thx ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('thx s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('s');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('thx sw');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('thx swe');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('thx swee');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('thx sweet');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('thx sweeti');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('thx sweetie');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-primary'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 18.5,
                y: 14,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-primary'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 18.5,
                y: 14,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/connections');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Max)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 163.5,
                y: 27.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Max)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 163.5,
                y: 27.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-footer'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen-footer\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='screen-footer'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 374.5,
                y: 203.390625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-footer'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen-footer\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='screen-footer'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 374.5,
                y: 203.390625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('he');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('hey');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('y');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('hey ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('hey u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Back)'),
            targetPage.locator("[data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 16.5,
                y: 32,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Back)'),
            targetPage.locator("[data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 16.5,
                y: 32,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/connections');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Geo)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 355.5,
                y: 40.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Geo)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 355.5,
                y: 40.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-footer'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen-footer\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='screen-footer'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 340.5,
                y: 202.390625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-footer'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen-footer\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='screen-footer'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 340.5,
                y: 202.390625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('he');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('hey');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('y');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('hey ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('hey u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('hey uà');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('à');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('hey u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Backspace');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('form > div:nth-of-type(3) path'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(':scope >>> form > div:nth-of-type(3) path')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12.18804931640625,
                y: 8.1875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('form > div:nth-of-type(3) path'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(':scope >>> form > div:nth-of-type(3) path')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12.18804931640625,
                y: 8.1875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-primary'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 22.5,
                y: 14,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-primary'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 22.5,
                y: 14,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/connections');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Profile) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='my-profile'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='my-profile'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 11.500244140625,
                y: 13.000244140625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Profile) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='my-profile'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='my-profile'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 11.500244140625,
                y: 13.000244140625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('span.csms-profile-info__age > span'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"own-profile-profile-info\\"]/div/span/span/span[2]/span)'),
            targetPage.locator(':scope >>> span.csms-profile-info__age > span'),
            targetPage.locator('::-p-text(, 29)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 7.9453125,
                y: 6.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('span.csms-profile-info__age > span'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"own-profile-profile-info\\"]/div/span/span/span[2]/span)'),
            targetPage.locator(':scope >>> span.csms-profile-info__age > span'),
            targetPage.locator('::-p-text(, 29)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 7.9453125,
                y: 6.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('circle.csms-progress-circle__svg-path-indicator'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"brick-action\\"]/span[2]/div/span/svg/circle[2])'),
            targetPage.locator(':scope >>> circle.csms-progress-circle__svg-path-indicator')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 70.44546508789062,
                y: 52.945457458496094,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('circle.csms-progress-circle__svg-path-indicator'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"brick-action\\"]/span[2]/div/span/svg/circle[2])'),
            targetPage.locator(':scope >>> circle.csms-progress-circle__svg-path-indicator')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 70.44546508789062,
                y: 52.945457458496094,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='profile-card-close'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='profile-card-close'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 7.5,
                y: 13,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='profile-card-close'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='profile-card-close'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 7.5,
                y: 13,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(99+ Likes)'),
            targetPage.locator("[data-qa='liked-you']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"liked-you\\"])'),
            targetPage.locator(":scope >>> [data-qa='liked-you']"),
            targetPage.locator('::-p-text(99+Likes)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 75.5,
                y: 17,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(99+ Likes)'),
            targetPage.locator("[data-qa='liked-you']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"liked-you\\"])'),
            targetPage.locator(":scope >>> [data-qa='liked-you']"),
            targetPage.locator('::-p-text(99+Likes)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 75.5,
                y: 17,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Profile) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='my-profile'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='my-profile'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 7.500244140625,
                y: 15.000244140625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Profile) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='my-profile'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='my-profile'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 7.500244140625,
                y: 15.000244140625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#tabbar path:nth-of-type(2)'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(':scope >>> #tabbar path:nth-of-type(2)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 11.49725341796875,
                y: 16.00054931640625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#tabbar path:nth-of-type(2)'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(':scope >>> #tabbar path:nth-of-type(2)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 11.49725341796875,
                y: 16.00054931640625,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Erion’s photo[role=\\"image\\"])'),
            targetPage.locator("[data-qa='first-card'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 465.5,
                y: 395,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Erion’s photo[role=\\"image\\"])'),
            targetPage.locator("[data-qa='first-card'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 465.5,
                y: 395,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Erion’s photo, 1 of 1)'),
            targetPage.locator("#js-fullscreen [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> #js-fullscreen [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 541,
                y: 374,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Erion’s photo, 1 of 1)'),
            targetPage.locator("#js-fullscreen [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> #js-fullscreen [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 541,
                y: 374,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Erion’s photo, 1 of 1)'),
            targetPage.locator("#js-fullscreen [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> #js-fullscreen [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 767,
                y: 100,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Erion’s photo, 1 of 1)'),
            targetPage.locator("#js-fullscreen [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> #js-fullscreen [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 767,
                y: 100,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Erion’s photo, 1 of 1)'),
            targetPage.locator("#js-fullscreen [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> #js-fullscreen [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 430,
                y: 186,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Erion’s photo, 1 of 1)'),
            targetPage.locator("#js-fullscreen [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> #js-fullscreen [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 430,
                y: 186,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Erion’s photo, 1 of 1)'),
            targetPage.locator("#js-fullscreen [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> #js-fullscreen [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 149,
                y: 86,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Erion’s photo, 1 of 1)'),
            targetPage.locator("#js-fullscreen [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> #js-fullscreen [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 149,
                y: 86,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Erion’s photo, 1 of 1)'),
            targetPage.locator("#js-fullscreen [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> #js-fullscreen [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 263,
                y: 95,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Erion’s photo, 1 of 1)'),
            targetPage.locator("#js-fullscreen [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> #js-fullscreen [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 263,
                y: 95,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 21.166656494140625,
                y: 2.26983642578125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 21.166656494140625,
                y: 2.26983642578125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 223,
                y: 22.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 223,
                y: 22.3984375,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 51.0013427734375,
                y: 26.105224609375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 51.0013427734375,
                y: 26.105224609375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 243,
                y: 37.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 243,
                y: 37.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='first-card'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='first-card'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 496.5,
                y: 332,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='first-card'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='first-card'] > div > div > div.profile-card-full__content > div > div.profile-card__content-scroller > div > div > [data-qa='user-section-photo'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 496.5,
                y: 332,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.slider-gallery svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> div.slider-gallery svg')
        ])
            .setTimeout(timeout)
            .click({
              delay: 366.3999999985099,
              offset: {
                x: 11,
                y: 17.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.slider-gallery svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> div.slider-gallery svg')
        ])
            .setTimeout(timeout)
            .click({
              delay: 366.3999999985099,
              offset: {
                x: 11,
                y: 17.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Massi’s photo, 2 of 2)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 828,
                y: 323,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Massi’s photo, 2 of 2)'),
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 828,
                y: 323,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12.166656494140625,
                y: 9.061859130859375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12.166656494140625,
                y: 9.061859130859375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 262,
                y: 25.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 262,
                y: 25.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 20.166656494140625,
                y: 10.105926513671875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 20.166656494140625,
                y: 10.105926513671875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 280,
                y: 34.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 280,
                y: 34.3984375,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 19.166656494140625,
                y: 12.7781982421875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 19.166656494140625,
                y: 12.7781982421875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 269,
                y: 28.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 269,
                y: 28.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 50.0013427734375,
                y: 30.7940673828125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 50.0013427734375,
                y: 30.7940673828125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-header'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='screen-header'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3.5,
                y: 9,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-header'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='screen-header'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3.5,
                y: 9,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 24.166656494140625,
                y: 8.2259521484375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 24.166656494140625,
                y: 8.2259521484375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 254,
                y: 35.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 254,
                y: 35.3984375,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 20.166656494140625,
                y: 21.539794921875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 20.166656494140625,
                y: 21.539794921875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 255,
                y: 27.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 255,
                y: 27.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 51.0013427734375,
                y: 33.426544189453125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 51.0013427734375,
                y: 33.426544189453125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 253,
                y: 25.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 253,
                y: 25.3984375,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 13.166656494140625,
                y: 11.77850341796875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(3) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 13.166656494140625,
                y: 11.77850341796875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 254,
                y: 19.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 254,
                y: 19.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(99+ Likes) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='liked-you'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='liked-you'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 9.500244140625,
                y: 15.0025634765625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(99+ Likes) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='liked-you'] path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='liked-you'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 9.500244140625,
                y: 15.0025634765625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.csms-screen__block--align-stretch > div > div > div:nth-of-type(1) span.csms-user-card-content__slots'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/button/span[2])'),
            targetPage.locator(':scope >>> div.csms-screen__block--align-stretch > div > div > div:nth-of-type(1) span.csms-user-card-content__slots')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 193.5,
                y: 225.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.csms-screen__block--align-stretch > div > div > div:nth-of-type(1) span.csms-user-card-content__slots'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/button/span[2])'),
            targetPage.locator(':scope >>> div.csms-screen__block--align-stretch > div > div > div:nth-of-type(1) span.csms-user-card-content__slots')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 193.5,
                y: 225.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('path:nth-of-type(3)'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(':scope >>> path:nth-of-type(3)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 16.166656494140625,
                y: 10.58758544921875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('path:nth-of-type(3)'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(':scope >>> path:nth-of-type(3)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 16.166656494140625,
                y: 10.58758544921875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Say Hello, Maazz)'),
            targetPage.locator('div.csms-screen__block--align-stretch > div > div > div:nth-of-type(1) button'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/div[2]/button)'),
            targetPage.locator(':scope >>> div.csms-screen__block--align-stretch > div > div > div:nth-of-type(1) button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 189.5,
                y: 18.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Say Hello, Maazz)'),
            targetPage.locator('div.csms-screen__block--align-stretch > div > div > div:nth-of-type(1) button'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"user-card\\"]/div[2]/div[2]/button)'),
            targetPage.locator(':scope >>> div.csms-screen__block--align-stretch > div > div > div:nth-of-type(1) button')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 189.5,
                y: 18.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Say Hello)'),
            targetPage.locator("[data-qa='chat-nudge-primary-button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-nudge-primary-button\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-nudge-primary-button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 399.5,
                y: 35.1875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Say Hello)'),
            targetPage.locator("[data-qa='chat-nudge-primary-button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-nudge-primary-button\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-nudge-primary-button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 399.5,
                y: 35.1875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Maazz’s photo, 1 of 2[role=\\"image\\"])'),
            targetPage.locator("div.mini-profile__gallery > div > div > div > div:nth-of-type(1) [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> div.mini-profile__gallery > div > div > div > div:nth-of-type(1) [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 63.5,
                y: 60,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Maazz’s photo, 1 of 2[role=\\"image\\"])'),
            targetPage.locator("div.mini-profile__gallery > div > div > div > div:nth-of-type(1) [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> div.mini-profile__gallery > div > div > div > div:nth-of-type(1) [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 63.5,
                y: 60,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.slider-gallery svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> div.slider-gallery svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 14,
                y: 14.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.slider-gallery svg'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(':scope >>> div.slider-gallery svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 14,
                y: 14.5,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/liked-you');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#tabbar path:nth-of-type(2)'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(':scope >>> #tabbar path:nth-of-type(2)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 10.49725341796875,
                y: 6.00054931640625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#tabbar path:nth-of-type(2)'),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(':scope >>> #tabbar path:nth-of-type(2)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 10.49725341796875,
                y: 6.00054931640625,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 43.0013427734375,
                y: 15.5013427734375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 43.0013427734375,
                y: 15.5013427734375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 283,
                y: 11.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 283,
                y: 11.3984375,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              delay: 388.3999999985099,
              offset: {
                x: 31.0013427734375,
                y: 25.5013427734375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              delay: 388.3999999985099,
              offset: {
                x: 31.0013427734375,
                y: 25.5013427734375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 9.53271484375,
                y: 7.022796630859375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 9.53271484375,
                y: 7.022796630859375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Undo) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='navbar-slot-secondary'] > div:nth-of-type(1) path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-secondary'] > div:nth-of-type(1) path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 1.50006103515625,
                y: 16,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Undo) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-qa='navbar-slot-secondary'] > div:nth-of-type(1) path"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-secondary'] > div:nth-of-type(1) path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 1.50006103515625,
                y: 16,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 31.0013427734375,
                y: 20.61260986328125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 31.0013427734375,
                y: 20.61260986328125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 30.166656494140625,
                y: 9.777862548828125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 30.166656494140625,
                y: 9.777862548828125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 26.166656494140625,
                y: 4.833953857421875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 26.166656494140625,
                y: 4.833953857421875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 27.166656494140625,
                y: 2.834228515625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 27.166656494140625,
                y: 2.834228515625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 22.166656494140625,
                y: 6.12762451171875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 22.166656494140625,
                y: 6.12762451171875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 47.0013427734375,
                y: 28.505645751953125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[2])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(2)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 47.0013427734375,
                y: 28.505645751953125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 22.166656494140625,
                y: 2.951904296875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 22.166656494140625,
                y: 2.951904296875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 18.53271484375,
                y: 10.001434326171875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] [data-qa='profile-action-list'] > div:nth-of-type(2) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 18.53271484375,
                y: 10.001434326171875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 21.166656494140625,
                y: 5.667022705078125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 21.166656494140625,
                y: 5.667022705078125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 250,
                y: 15.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 250,
                y: 15.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 22.166656494140625,
                y: 6.68841552734375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg/path[3])'),
            targetPage.locator(":scope >>> [data-qa='first-card'] div:nth-of-type(4) path:nth-of-type(3)")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 22.166656494140625,
                y: 6.68841552734375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 259,
                y: 29.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send A Quick ‘Hello’)'),
            targetPage.locator("[data-qa='mutual-attraction-cta']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"mutual-attraction-cta\\"])'),
            targetPage.locator(":scope >>> [data-qa='mutual-attraction-cta']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 259,
                y: 29.3984375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='connections'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='connections'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 22.5,
                y: 11,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='connections'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='connections'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 22.5,
                y: 11,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Nicolai, Online now)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 107.5,
                y: 19.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Nicolai, Online now)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 107.5,
                y: 19.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-footer'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen-footer\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='screen-footer'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 284.5,
                y: 208.390625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-footer'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen-footer\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='screen-footer'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 284.5,
                y: 208.390625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('ho');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('how');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('w');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('how ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('how r');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('r');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('how r ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('how r u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('how r u ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('how r u d');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('d');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('how r u do');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('o');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('how r u doi');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('how r u doin');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('how r u doing');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('g');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('how r u doing ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('how r u doing ?');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Shift');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up("'");
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up("'");
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-primary'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 14.5,
                y: 20,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-primary'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 14.5,
                y: 20,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/connections');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Maazz, Online now)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 139.5,
                y: 32.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Maazz, Online now)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 139.5,
                y: 32.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('m');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('m');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('m');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('m');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('f');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('f');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('f');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('f');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-footer'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen-footer\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='screen-footer'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 334.5,
                y: 194.390625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen-footer'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen-footer\\"]/div)'),
            targetPage.locator(":scope >>> [data-qa='screen-footer'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 334.5,
                y: 194.390625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('f');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('f');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fi');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('i');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fin');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('e');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('t');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine th');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('h');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('x');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('n');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('u');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n u ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up(' ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('fine thx n u ?');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Shift');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up("'");
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up("'");
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Back)'),
            targetPage.locator("[data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 18.5,
                y: 32,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Back)'),
            targetPage.locator("[data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 18.5,
                y: 32,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/connections');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='my-profile'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='my-profile'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 21.5,
                y: 21,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='my-profile'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='my-profile'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 21.5,
                y: 21,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='people-nearby'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='people-nearby'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 16.5,
                y: 18,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/settings-interface-language', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='people-nearby'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='people-nearby'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 16.5,
                y: 18,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'chrome-extension://abpdnfjocnmdomablahdcfnoggeeiedb/legacy/0.1.8/devtool.app.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Beautify JS,HTML,CSS code before download.)'),
            targetPage.locator('#check-beautify'),
            targetPage.locator('::-p-xpath(//*[@id=\\"check-beautify\\"])'),
            targetPage.locator(':scope >>> #check-beautify')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12,
                y: 12.5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'chrome-extension://abpdnfjocnmdomablahdcfnoggeeiedb/legacy/0.1.8/devtool.app.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Include all assets by XHR requests \\(require page reload\\).)'),
            targetPage.locator('#check-xhr'),
            targetPage.locator('::-p-xpath(//*[@id=\\"check-xhr\\"])'),
            targetPage.locator(':scope >>> #check-xhr')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 6,
                y: 10,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'chrome-extension://abpdnfjocnmdomablahdcfnoggeeiedb/legacy/0.1.8/devtool.app.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Save All Resources)'),
            targetPage.locator('#up-save'),
            targetPage.locator('::-p-xpath(//*[@id=\\"up-save\\"])'),
            targetPage.locator(':scope >>> #up-save'),
            targetPage.locator('::-p-text(Save All Resources)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 106.5859375,
                y: 23.25,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/people-nearby', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Say Hello Now)'),
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div.csms-modal__scrollable > [data-qa='cta-box'] > [data-qa='cta-box-buttons'] > [data-qa='csms-stack'] > [data-qa='csms-stack-item'] > [data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div.csms-modal__scrollable > [data-qa='cta-box'] > [data-qa='cta-box-buttons'] > [data-qa='csms-stack'] > [data-qa='csms-stack-item'] > [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 96.5,
                y: 24.796875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/people-nearby', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='connections'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='connections'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 21.5,
                y: 21,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/encounters');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/people-nearby', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Tyrese, Online now)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 251.5,
                y: 28.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/people-nearby', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='loader-screen']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"loader-screen\\"])'),
            targetPage.locator(":scope >>> [data-qa='loader-screen']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 457,
                y: 391,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'chrome-extension://pbfecmmdbppphcnmlmegkcdobpadegid/index.html', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Filter)'),
            targetPage.locator('input'),
            targetPage.locator('::-p-xpath(//*[@id=\\"root\\"]/div/div/div[1]/div/input)'),
            targetPage.locator(':scope >>> input')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 289,
                y: 6,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chromewebstore.google.com/detail/local-storage-inspector/pbfecmmdbppphcnmlmegkcdobpadegid', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.qdOxv-fmcmS-yrriRe-OWXEXe-H9tDt > div'),
            targetPage.locator('::-p-xpath(//*[@id=\\"kO001e\\"]/div/div[1]/div[2]/div/div[1]/div)'),
            targetPage.locator(':scope >>> div.qdOxv-fmcmS-yrriRe-OWXEXe-H9tDt > div')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 366.7109375,
                y: 1,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/people-nearby', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('body'),
            targetPage.locator('::-p-xpath(/html/body)'),
            targetPage.locator(':scope >>> body')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 826,
                y: 43,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('chrome://serviceworker-internals/?devtools');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'chrome://serviceworker-internals/?devtools', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div:nth-of-type(3) div.serviceworker-top-level-site'),
            targetPage.locator('::-p-xpath(//*[@id=\\"serviceworker-list\\"]/div/div[3]/div/div[2]/div/div[2])'),
            targetPage.locator(':scope >>> div:nth-of-type(3) div.serviceworker-top-level-site')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 1096,
                y: 4.84375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/people-nearby', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='navbar-slot-primary'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 3.5,
                y: 14,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/connections');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/people-nearby', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Catalin, Online now , CHAT REQUEST)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-text(CatalinCatalin, Online now,CHAT)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12.5,
                y: 24.0078125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/people-nearby', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Catalin’s photo, 1 of 2[role=\\"image\\"])'),
            targetPage.locator("div.mini-profile__gallery > div > div > div > div:nth-of-type(1) [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> div.mini-profile__gallery > div > div > div > div:nth-of-type(1) [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 20.5,
                y: 76,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/people-nearby', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 30,
                y: 35,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/people-nearby', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 91,
                y: 118,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/people-nearby', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"multimedia-image\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #js-fullscreen > [data-qa='fullscreen-gallery'] > div.slider-gallery > div.slider-gallery-pages > [data-qa='slider-gallery-page'] [data-qa='multimedia-image']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 30,
                y: 63,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/people-nearby', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Catalin, Online now)'),
            targetPage.locator("html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"connections-item-content\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > div > div > [data-qa='screen'] > div.csms-screen__block--align-stretch > [data-qa='connections-content'] > div.connections__items > div.connections-items > div > ul > [data-qa='connections-list-item'] [data-qa='connections-item-content']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 45.5,
                y: 22.40625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/people-nearby', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='screen']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"screen\\"])'),
            targetPage.locator(":scope >>> [data-qa='screen']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 199.5,
                y: 570,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/people-nearby', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('im fine thx n u?');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/people-nearby', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up("'");
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/people-nearby', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Type a message...)'),
            targetPage.locator("[data-qa='chat-input-textarea']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"chat-input-textarea\\"])'),
            targetPage.locator(":scope >>> [data-qa='chat-input-textarea']")
        ])
            .setTimeout(timeout)
            .fill('im fine thx n u?');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/people-nearby', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/people-nearby', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/people-nearby', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Back)'),
            targetPage.locator("[data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"navbar-icon\\"])'),
            targetPage.locator(":scope >>> [data-qa='navbar-slot-primary'] > [data-qa='navbar-icon']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 6.5,
                y: 26,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://am1.moumi.com/en-us/connections');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/people-nearby', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-qa='my-profile'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"icon\\"]/svg)'),
            targetPage.locator(":scope >>> [data-qa='my-profile'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 4.5,
                y: 13,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/people-nearby', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(2) > [data-qa='action-row'] [data-qa='action-row-title'] > span"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"action-row-title\\"]/span)'),
            targetPage.locator(":scope >>> html > body > #app-root > div > #page-container > div > [data-qa='screen'] > div:nth-of-type(2) > [data-qa='action-row'] [data-qa='action-row-title'] > span"),
            targetPage.locator('::-p-text(Account)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 25.5,
                y: 18.8046875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/people-nearby', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Log out)'),
            targetPage.locator("[data-qa='sign-out-button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"sign-out-button\\"])'),
            targetPage.locator(":scope >>> [data-qa='sign-out-button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 113.5,
                y: 28.203125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://am1.moumi.com/en-us/people-nearby', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria([role=\\"dialog\\"]) >>>> ::-p-aria(Log out)'),
            targetPage.locator("html > body > #app-root > div > div.modal-container > div > div > div > div > div > [data-qa='signout-passkey-confirmation'] > [data-qa='action-sheet-list'] > [data-qa='action-sheet-list-item'] > span > [data-qa='cta-box'] > [data-qa='cta-box-buttons'] > [data-qa='csms-stack'] > [data-qa='csms-stack-item'] > [data-qa='button']"),
            targetPage.locator('::-p-xpath(//*[@data-qa=\\"button\\"])'),
            targetPage.locator(":scope >>> html > body > #app-root > div > div.modal-container > div > div > div > div > div > [data-qa='signout-passkey-confirmation'] > [data-qa='action-sheet-list'] > [data-qa='action-sheet-list-item'] > span > [data-qa='cta-box'] > [data-qa='cta-box-buttons'] > [data-qa='csms-stack'] > [data-qa='csms-stack-item'] > [data-qa='button']")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 123.5,
                y: 17.3984375,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://www.google.com/search?q=sissy+cucklod+erotic+stories&oq=sissy+cucklod+erotic+stories&gs_lcrp=EgZjaHJvbWUyBggAEEUYOdIBCTEyMDE4ajBqNKgCALACAQ&sourceid=chrome&ie=UTF-8');
    }
    await lhFlow.endNavigation();
    await lhFlow.startNavigation();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://www.google.com/search?q=sissy+cucklod+erotic+stories&oq=sissy+cucklod+erotic+stories&gs_lcrp=EgZjaHJvbWUyBggAEEUYOdIBCTEyMDE4ajBqNKgCALACAQ&sourceid=chrome&ie=UTF-8&sei=vbRkaIPIN-Hwi-gPjYvdwQQ', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        const promises = [];
        const startWaitingForEvents = () => {
            promises.push(targetPage.waitForNavigation());
        }
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Stories by SissyCuckold | Darkwanderer - Cuckold forums)'),
            targetPage.locator('div:nth-of-type(5) h3'),
            targetPage.locator('::-p-xpath(//*[@id=\\"rso\\"]/div[2]/div[5]/div/div/div/div[1]/div/div/span/a/h3)'),
            targetPage.locator(':scope >>> div:nth-of-type(5) h3'),
            targetPage.locator('::-p-text(Stories by SissyCuckold)')
        ])
            .setTimeout(timeout)
            .on('action', () => startWaitingForEvents())
            .click({
              offset: {
                x: 39,
                y: 12.265625,
              },
            });
        await Promise.all(promises);
    }
    await lhFlow.endNavigation();
    await lhFlow.startNavigation();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://darkwanderer.net/ams/authors/sissycuckold.450/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        const promises = [];
        const startWaitingForEvents = () => {
            promises.push(targetPage.waitForNavigation());
        }
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(My Ex’s Big Sister)'),
            targetPage.locator('div.js-articleListItem-8378 div.structItem-title > a'),
            targetPage.locator('::-p-xpath(//*[@id=\\"top\\"]/div[4]/div/div[2]/div[2]/div/div/div[2]/div[2]/div/div[3]/div[2]/div[2]/a)'),
            targetPage.locator(':scope >>> div.js-articleListItem-8378 div.structItem-title > a'),
            targetPage.locator('::-p-text(My Ex’s Big Sister)')
        ])
            .setTimeout(timeout)
            .on('action', () => startWaitingForEvents())
            .click({
              offset: {
                x: 34.5,
                y: 10.84375,
              },
            });
        await Promise.all(promises);
    }
    await lhFlow.endNavigation();
    await lhFlow.startNavigation();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://darkwanderer.net/ams/my-ex%E2%80%99s-big-sister.8378/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        const promises = [];
        const startWaitingForEvents = () => {
            promises.push(targetPage.waitForNavigation());
        }
        await puppeteer.Locator.race([
            targetPage.locator('div.p-body > div > ul:nth-of-type(1) > li:nth-of-type(3) span'),
            targetPage.locator('::-p-xpath(//*[@id=\\"top\\"]/div[4]/div/ul[1]/li[3]/a/span)'),
            targetPage.locator(':scope >>> div.p-body > div > ul:nth-of-type(1) > li:nth-of-type(3) span')
        ])
            .setTimeout(timeout)
            .on('action', () => startWaitingForEvents())
            .click({
              offset: {
                x: 49.9765625,
                y: 5.8046875,
              },
            });
        await Promise.all(promises);
    }
    await lhFlow.endNavigation();
    await lhFlow.startNavigation();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://darkwanderer.net/ams/categories/cuckold-stories.1/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        const promises = [];
        const startWaitingForEvents = () => {
            promises.push(targetPage.waitForNavigation());
        }
        await puppeteer.Locator.race([
            targetPage.locator('div.js-articleListItem-18466 div.structItem-title > a'),
            targetPage.locator('::-p-xpath(//*[@id=\\"top\\"]/div[4]/div/div[2]/div[5]/div/div/div[2]/div[2]/div/div/div[7]/div[2]/div[1]/a)'),
            targetPage.locator(':scope >>> div.js-articleListItem-18466 div.structItem-title > a')
        ])
            .setTimeout(timeout)
            .on('action', () => startWaitingForEvents())
            .click({
              offset: {
                x: 12.5,
                y: 16.21875,
              },
            });
        await Promise.all(promises);
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://darkwanderer.net/ams/from-husband-to-beta-on-a-beach.18466/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Back)'),
            targetPage.locator('div.u-navButtons > a'),
            targetPage.locator('::-p-xpath(//*[@id=\\"XF\\"]/body/div[3]/a)'),
            targetPage.locator(':scope >>> div.u-navButtons > a')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 8,
                y: 19.1953125,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://darkwanderer.net/ams/categories/cuckold-stories.1/');
    }
    await lhFlow.endNavigation();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://chatgpt.com/');
    }
    await lhFlow.endNavigation();
    await lhFlow.startTimespan();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria([role=\\"region\\"]) >>>> ::-p-aria([role=\\"paragraph\\"])'),
            targetPage.locator('div.h-full p'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"]/p)'),
            targetPage.locator(':scope >>> div.h-full p')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 364,
                y: 8.3046875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#prompt-textarea'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"])'),
            targetPage.locator(':scope >>> #prompt-textarea')
        ])
            .setTimeout(timeout)
            .fill('how to make an app access storage whilst it gets stuck whenever it needs to access storage to set a directory for its function.');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Shift');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Shift');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#prompt-textarea'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"])'),
            targetPage.locator(':scope >>> #prompt-textarea')
        ])
            .setTimeout(timeout)
            .fill('how to make an app access storage whilst it gets stuck whenever it needs to access storage to set a directory for its function.??');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Shift');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Shift');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#prompt-textarea'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"])'),
            targetPage.locator(':scope >>> #prompt-textarea')
        ])
            .setTimeout(timeout)
            .fill('how to make an app access storage whilst it gets stuck whenever it needs to access storage to set a directory for its function.??all permissions granted , all appops allowed !');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Dead');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#prompt-textarea'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"])'),
            targetPage.locator(':scope >>> #prompt-textarea')
        ])
            .setTimeout(timeout)
            .fill('im on 9');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Stop streaming) >>>> ::-p-aria([role=\\"graphics-symbol\\"])'),
            targetPage.locator("[data-testid='composer-trailing-actions'] path"),
            targetPage.locator('::-p-xpath(//*[@data-testid=\\"stop-button\\"]/svg/path)'),
            targetPage.locator(":scope >>> [data-testid='composer-trailing-actions'] path")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 4.5999755859375,
                y: 7.5999755859375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send prompt) >>>> ::-p-aria([role=\\"image\\"])'),
            targetPage.locator("[data-testid='composer-trailing-actions'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-testid=\\"send-button\\"]/svg)'),
            targetPage.locator(":scope >>> [data-testid='composer-trailing-actions'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 7,
                y: 9,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Message Composer) >>>> ::-p-aria([role=\\"paragraph\\"])'),
            targetPage.locator('#thread-bottom-container p'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"]/p)'),
            targetPage.locator(':scope >>> #thread-bottom-container p')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 266,
                y: 2,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#prompt-textarea'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"])'),
            targetPage.locator(':scope >>> #prompt-textarea')
        ])
            .setTimeout(timeout)
            .fill('isnt there a way to set this option via cmd or to force it go through by cmd ?');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up("'");
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#prompt-textarea'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"])'),
            targetPage.locator(':scope >>> #prompt-textarea')
        ])
            .setTimeout(timeout)
            .fill('isnt there a way to set this option via cmd or to force it go through by cmd ? u know one of those no way to understand intents may be ??');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#thread-bottom-container > div.text-token-text-secondary'),
            targetPage.locator('::-p-xpath(//*[@id=\\"thread-bottom-container\\"]/div[2])'),
            targetPage.locator(':scope >>> #thread-bottom-container > div.text-token-text-secondary')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 1,
                y: 5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-testid='conversation-turn-6'] pre:nth-of-type(1) div > button"),
            targetPage.locator('::-p-xpath(//*[@data-testid=\\"conversation-turn-6\\"]/div/div/div/div/div[1]/div/div/div/pre[1]/div/div[2]/div/div/button)'),
            targetPage.locator(":scope >>> [data-testid='conversation-turn-6'] pre:nth-of-type(1) div > button")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 25.4375,
                y: 17.0078125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-testid='conversation-turn-6'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-testid=\\"conversation-turn-6\\"]/div)'),
            targetPage.locator(":scope >>> [data-testid='conversation-turn-6'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 1146,
                y: 547,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-testid='conversation-turn-6'] pre:nth-of-type(2) div > button"),
            targetPage.locator('::-p-xpath(//*[@data-testid=\\"conversation-turn-6\\"]/div/div/div/div/div[1]/div/div/div/pre[2]/div/div[2]/div/div/button)'),
            targetPage.locator(":scope >>> [data-testid='conversation-turn-6'] pre:nth-of-type(2) div > button")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 16.4375,
                y: 16.0078125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-testid='conversation-turn-6'] pre:nth-of-type(3) div > button"),
            targetPage.locator('::-p-xpath(//*[@data-testid=\\"conversation-turn-6\\"]/div/div/div/div/div[1]/div/div/div/pre[3]/div/div[2]/div/div/button)'),
            targetPage.locator(":scope >>> [data-testid='conversation-turn-6'] pre:nth-of-type(3) div > button")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 31.4375,
                y: 7.0078125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-testid='conversation-turn-6'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-testid=\\"conversation-turn-6\\"]/div)'),
            targetPage.locator(":scope >>> [data-testid='conversation-turn-6'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 1138,
                y: 1023,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Message Composer) >>>> ::-p-aria([role=\\"paragraph\\"])'),
            targetPage.locator('#thread-bottom-container p'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"]/p)'),
            targetPage.locator(':scope >>> #thread-bottom-container p')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 268,
                y: 7,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#prompt-textarea'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"])'),
            targetPage.locator(':scope >>> #prompt-textarea')
        ])
            .setTimeout(timeout)
            .fill('no uid from the app in user 0 !?');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up("'");
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#prompt-textarea'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"])'),
            targetPage.locator(':scope >>> #prompt-textarea')
        ])
            .setTimeout(timeout)
            .fill('lol but it does ..i am solo user');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Stay logged out)'),
            targetPage.locator("[data-testid='modal-no-auth-rate-limit'] a"),
            targetPage.locator('::-p-xpath(//*[@id=\\"radix-«r22»\\"]/div/div/a)'),
            targetPage.locator(":scope >>> [data-testid='modal-no-auth-rate-limit'] a"),
            targetPage.locator('::-p-text(Stay logged out)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 77.5,
                y: 18.1171875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-testid='conversation-turn-10'] pre:nth-of-type(3) div > button"),
            targetPage.locator('::-p-xpath(//*[@data-testid=\\"conversation-turn-10\\"]/div/div/div/div/div[1]/div/div/div/pre[3]/div/div[2]/div/div/button)'),
            targetPage.locator(":scope >>> [data-testid='conversation-turn-10'] pre:nth-of-type(3) div > button")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 25.4375,
                y: 6.546875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Message Composer) >>>> ::-p-aria([role=\\"paragraph\\"])'),
            targetPage.locator('#thread-bottom-container p'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"]/p)'),
            targetPage.locator(':scope >>> #thread-bottom-container p')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 20,
                y: 2,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#prompt-textarea'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"])'),
            targetPage.locator(':scope >>> #prompt-textarea')
        ])
            .setTimeout(timeout)
            .fill('how can i first force adb to listen to usb only =');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('0');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#prompt-textarea'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"])'),
            targetPage.locator(':scope >>> #prompt-textarea')
        ])
            .setTimeout(timeout)
            .fill('how can i first force adb to listen to usb only ?');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up("'");
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-testid='conversation-turn-12'] pre:nth-of-type(1) div > button"),
            targetPage.locator('::-p-xpath(//*[@data-testid=\\"conversation-turn-12\\"]/div/div/div/div/div/div/div/div/pre[1]/div/div[2]/div/div/button)'),
            targetPage.locator(":scope >>> [data-testid='conversation-turn-12'] pre:nth-of-type(1) div > button")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 24.4375,
                y: 13.578125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-testid='conversation-turn-7'] div.max-w-full > div > div > div"),
            targetPage.locator('::-p-xpath(//*[@data-testid=\\"conversation-turn-7\\"]/div/div/div/div/div[1]/div/div/div)'),
            targetPage.locator(":scope >>> [data-testid='conversation-turn-7'] div.max-w-full > div > div > div")
        ])
            .setTimeout(timeout)
            .click({
              delay: 558.6999999992549,
              offset: {
                x: 250.46875,
                y: 21.015625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-testid='conversation-turn-7'] div.max-w-full > div > div > div > div"),
            targetPage.locator('::-p-xpath(//*[@data-testid=\\"conversation-turn-7\\"]/div/div/div/div/div[1]/div/div/div/div)'),
            targetPage.locator(":scope >>> [data-testid='conversation-turn-7'] div.max-w-full > div > div > div > div"),
            targetPage.locator('::-p-text(no uid from the)')
        ])
            .setTimeout(timeout)
            .click({
              button: 'right',
              offset: {
                x: 154.46875,
                y: 15.015625,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Message Composer) >>>> ::-p-aria([role=\\"paragraph\\"])'),
            targetPage.locator('#thread-bottom-container p'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"]/p)'),
            targetPage.locator(':scope >>> #thread-bottom-container p')
        ])
            .setTimeout(timeout)
            .click({
              button: 'right',
              offset: {
                x: 104,
                y: 10,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(microphone[role=\\"image\\"])'),
            targetPage.locator('div.ml-3 img'),
            targetPage.locator('::-p-xpath(//*[@id=\\"vc-record-button\\"]/img)'),
            targetPage.locator(':scope >>> div.ml-3 img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 18,
                y: 11,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-testid='conversation-turn-14'] pre:nth-of-type(1) div > button"),
            targetPage.locator('::-p-xpath(//*[@data-testid=\\"conversation-turn-14\\"]/div/div/div/div/div/div/div/div/pre[1]/div/div[2]/div/div/button)'),
            targetPage.locator(":scope >>> [data-testid='conversation-turn-14'] pre:nth-of-type(1) div > button")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 33.4375,
                y: 6.1171875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-testid='conversation-turn-14'] pre:nth-of-type(2) div > button"),
            targetPage.locator('::-p-xpath(//*[@data-testid=\\"conversation-turn-14\\"]/div/div/div/div/div/div/div/div/pre[2]/div/div[2]/div/div/button)'),
            targetPage.locator(":scope >>> [data-testid='conversation-turn-14'] pre:nth-of-type(2) div > button")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 17.4375,
                y: 12.1171875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(microphone[role=\\"image\\"])'),
            targetPage.locator('div.ml-3 img'),
            targetPage.locator('::-p-xpath(//*[@id=\\"vc-record-button\\"]/img)'),
            targetPage.locator(':scope >>> div.ml-3 img')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 9,
                y: 9,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-testid='composer-footer-actions']"),
            targetPage.locator('::-p-xpath(//*[@data-testid=\\"composer-footer-actions\\"])'),
            targetPage.locator(":scope >>> [data-testid='composer-footer-actions']"),
            targetPage.locator('::-p-text(AttachSearch)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 374,
                y: 2,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Message Composer) >>>> ::-p-aria([role=\\"paragraph\\"])'),
            targetPage.locator('#thread-bottom-container p'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"]/p)'),
            targetPage.locator(':scope >>> #thread-bottom-container p')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 361,
                y: 68,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#prompt-textarea'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"])'),
            targetPage.locator(':scope >>> #prompt-textarea')
        ])
            .setTimeout(timeout)
            .fill('number two is not the point because I can take any existing folder here already in the app it will not impose anything it just needs to get into the storage this is why I was thinking maybe this option in document provider and mix the floor could help but dunno how ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Message Composer) >>>> ::-p-aria([role=\\"paragraph\\"])'),
            targetPage.locator('#thread-bottom-container p'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"]/p)'),
            targetPage.locator(':scope >>> #thread-bottom-container p')
        ])
            .setTimeout(timeout)
            .click({
              delay: 1532.1000000014901,
              offset: {
                x: 189,
                y: 52,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#prompt-textarea'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"])'),
            targetPage.locator(':scope >>> #prompt-textarea')
        ])
            .setTimeout(timeout)
            .fill('number two is not the point because I can take any existing folder here already in the app it will not impose anything it just needs to get into the storage this is why I was thinking maybe this option in document provider and mixplore could help but dunno how ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Send prompt) >>>> ::-p-aria([role=\\"image\\"])'),
            targetPage.locator("[data-testid='composer-trailing-actions'] svg"),
            targetPage.locator('::-p-xpath(//*[@data-testid=\\"send-button\\"]/svg)'),
            targetPage.locator(":scope >>> [data-testid='composer-trailing-actions'] svg")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 19,
                y: 1,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-testid='conversation-turn-14'] > div"),
            targetPage.locator('::-p-xpath(//*[@data-testid=\\"conversation-turn-14\\"]/div)'),
            targetPage.locator(":scope >>> [data-testid='conversation-turn-14'] > div")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 1139,
                y: 389.109375,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-testid='conversation-turn-14'] pre:nth-of-type(1) div > button"),
            targetPage.locator('::-p-xpath(//*[@data-testid=\\"conversation-turn-14\\"]/div/div/div/div/div[1]/div/div/div/pre[1]/div/div[2]/div/div/button)'),
            targetPage.locator(":scope >>> [data-testid='conversation-turn-14'] pre:nth-of-type(1) div > button")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 16.4375,
                y: 7.6171875,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#thread-bottom > div'),
            targetPage.locator('::-p-xpath(//*[@id=\\"thread-bottom\\"]/div)'),
            targetPage.locator(':scope >>> #thread-bottom > div')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 1160,
                y: 24,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Message Composer) >>>> ::-p-aria([role=\\"paragraph\\"])'),
            targetPage.locator('#thread-bottom-container p'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"]/p)'),
            targetPage.locator(':scope >>> #thread-bottom-container p')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 208,
                y: 13,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#prompt-textarea'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"])'),
            targetPage.locator(':scope >>> #prompt-textarea')
        ])
            .setTimeout(timeout)
            .fill('hohohoo my activity bonjour ..i can run adb and thats it');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#prompt-textarea'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"])'),
            targetPage.locator(':scope >>> #prompt-textarea')
        ])
            .setTimeout(timeout)
            .fill('i asked u to spoof it via cmds u didnt !!');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Dead');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#prompt-textarea'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"])'),
            targetPage.locator(':scope >>> #prompt-textarea')
        ])
            .setTimeout(timeout)
            .fill('i asked u to spoof it via cmds u didnt !! i am root and i have intent x : is intent resolver');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-testid='conversation-turn-20'] pre:nth-of-type(2) div > button"),
            targetPage.locator('::-p-xpath(//*[@data-testid=\\"conversation-turn-20\\"]/div/div/div/div/div[1]/div/div/div/pre[2]/div/div[2]/div/div/button)'),
            targetPage.locator(":scope >>> [data-testid='conversation-turn-20'] pre:nth-of-type(2) div > button")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 22.4375,
                y: 6,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-testid='conversation-turn-20'] pre:nth-of-type(3) div > button"),
            targetPage.locator('::-p-xpath(//*[@data-testid=\\"conversation-turn-20\\"]/div/div/div/div/div[1]/div/div/div/pre[3]/div/div[2]/div/div/button)'),
            targetPage.locator(":scope >>> [data-testid='conversation-turn-20'] pre:nth-of-type(3) div > button")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 13.4375,
                y: 10.203125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Message Composer) >>>> ::-p-aria([role=\\"paragraph\\"])'),
            targetPage.locator('#thread-bottom-container p'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"]/p)'),
            targetPage.locator(':scope >>> #thread-bottom-container p')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 155,
                y: 5,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator("[data-testid='conversation-turn-20'] pre:nth-of-type(3) div > button"),
            targetPage.locator('::-p-xpath(//*[@data-testid=\\"conversation-turn-20\\"]/div/div/div/div/div[1]/div/div/div/pre[3]/div/div[2]/div/div/button)'),
            targetPage.locator(":scope >>> [data-testid='conversation-turn-20'] pre:nth-of-type(3) div > button")
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 16.4375,
                y: 10.203125,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Message Composer) >>>> ::-p-aria([role=\\"paragraph\\"])'),
            targetPage.locator('#thread-bottom-container p'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"]/p)'),
            targetPage.locator(':scope >>> #thread-bottom-container p')
        ])
            .setTimeout(timeout)
            .click({
              button: 'right',
              offset: {
                x: 175,
                y: 1,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#thread-bottom-container p:nth-of-type(3)'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"]/p[3])'),
            targetPage.locator(':scope >>> #thread-bottom-container p:nth-of-type(3)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 433,
                y: 8,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Shift');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Shift');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#prompt-textarea'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"])'),
            targetPage.locator(':scope >>> #prompt-textarea')
        ])
            .setTimeout(timeout)
            .fill('adb shell am start -a android.intent.action.OPEN_DOCUMENT_TREE \\\r    -d content://com.android.externalstorage.documents/tree/primary%3ADownload \\\r    --grant-read-uri-permission --grant-write-uri-permissionthis powerfully opens the storage , how to link it to open when i click the activity in the app? ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up("'");
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Message Composer) >>>> ::-p-aria([role=\\"paragraph\\"])'),
            targetPage.locator('#thread-bottom-container p'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"]/p)'),
            targetPage.locator(':scope >>> #thread-bottom-container p')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 94,
                y: 3,
              },
            });
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#prompt-textarea'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"])'),
            targetPage.locator(':scope >>> #prompt-textarea')
        ])
            .setTimeout(timeout)
            .fill('i thought thats where a tool holds the intent and redirect it ??');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up("'");
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('#prompt-textarea'),
            targetPage.locator('::-p-xpath(//*[@id=\\"prompt-textarea\\"])'),
            targetPage.locator(':scope >>> #prompt-textarea')
        ])
            .setTimeout(timeout)
            .fill('i damn have one ..i just need to know how ');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.down('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://chatgpt.com/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await targetPage.keyboard.up('Enter');
    }
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://darkwanderer.net/ams/categories/cuckold-stories.1/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        await puppeteer.Locator.race([
            targetPage.locator('div.js-articleListItem-18496 div.structItem-title > a'),
            targetPage.locator('::-p-xpath(//*[@id=\\"top\\"]/div[4]/div/div[2]/div[5]/div/div/div[2]/div[2]/div/div/div[4]/div[2]/div[1]/a)'),
            targetPage.locator(':scope >>> div.js-articleListItem-18496 div.structItem-title > a')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 165.5,
                y: 16.78125,
              },
            });
    }
    await lhFlow.endTimespan();
    await lhFlow.startNavigation();
    {
        const targetPage = page;
        await targetPage.goto('https://darkwanderer.net/ams/categories/cuckold-stories.1/');
    }
    await lhFlow.endNavigation();
    await lhFlow.startNavigation();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://darkwanderer.net/ams/categories/cuckold-stories.1/', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        const promises = [];
        const startWaitingForEvents = () => {
            promises.push(targetPage.waitForNavigation());
        }
        await puppeteer.Locator.race([
            targetPage.locator('div.p-body-content > div > div > div:nth-of-type(1) li:nth-of-type(2) > a'),
            targetPage.locator('::-p-xpath(//*[@id=\\"top\\"]/div[4]/div/div[2]/div[5]/div/div/div[1]/div/nav/div[1]/ul/li[2]/a)'),
            targetPage.locator(':scope >>> div.p-body-content > div > div > div:nth-of-type(1) li:nth-of-type(2) > a')
        ])
            .setTimeout(timeout)
            .on('action', () => startWaitingForEvents())
            .click({
              offset: {
                x: 4.265625,
                y: 15.515625,
              },
            });
        await Promise.all(promises);
    }
    await lhFlow.endNavigation();
    await lhFlow.startNavigation();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://darkwanderer.net/ams/categories/cuckold-stories.1/?page=2', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        const promises = [];
        const startWaitingForEvents = () => {
            promises.push(targetPage.waitForNavigation());
        }
        await puppeteer.Locator.race([
            targetPage.locator('div.block-outer--after li:nth-of-type(3) > a'),
            targetPage.locator('::-p-xpath(//*[@id=\\"top\\"]/div[4]/div/div[2]/div[5]/div/div/div[3]/div/nav/div[1]/ul/li[3]/a)'),
            targetPage.locator(':scope >>> div.block-outer--after li:nth-of-type(3) > a')
        ])
            .setTimeout(timeout)
            .on('action', () => startWaitingForEvents())
            .click({
              offset: {
                x: 22.203125,
                y: 18.25,
              },
            });
        await Promise.all(promises);
    }
    await lhFlow.endNavigation();
    await lhFlow.startNavigation();
    {
        const target = await browser.waitForTarget(t => t.url() === 'https://darkwanderer.net/ams/categories/cuckold-stories.1/?page=3', { timeout });
        const targetPage = await target.page();
        targetPage.setDefaultTimeout(timeout);
        const promises = [];
        const startWaitingForEvents = () => {
            promises.push(targetPage.waitForNavigation());
        }
        await puppeteer.Locator.race([
            targetPage.locator('div.js-articleListItem-18392 div.structItem-title > a'),
            targetPage.locator('::-p-xpath(//*[@id=\\"top\\"]/div[4]/div/div[2]/div[5]/div/div/div[2]/div[2]/div/div/div[3]/div[2]/div[1]/a)'),
            targetPage.locator(':scope >>> div.js-articleListItem-18392 div.structItem-title > a'),
            targetPage.locator('::-p-text(Crossdressers)')
        ])
            .setTimeout(timeout)
            .on('action', () => startWaitingForEvents())
            .click({
              offset: {
                x: 49.5,
                y: 9.1171875,
              },
            });
        await Promise.all(promises);
    }
    await lhFlow.endNavigation();
    const lhFlowReport = await lhFlow.generateReport();
    fs.writeFileSync(__dirname + '/flow.report.html', lhFlowReport)

    await browser.close();

})().catch(err => {
    console.error(err);
    process.exit(1);
});
